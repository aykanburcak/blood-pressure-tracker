---
phase: 06-tokens-and-typography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/theme/tokens.ts
  - src/lib/theme/index.ts
  - src/app/_layout.tsx
  - src/test/setup.ts
requirements:
  - DS-01
  - DS-02
  - DS-03
autonomous: true
---

<objective>
Ship Stitch-aligned **semantic colors**, **typography with Manrope + Inter**, and **updated radius/shadow** in `src/lib/theme`, plus **root font loading** with splash gating. Legacy `colors.*` / `typography` consumers keep working via aliases and `fontFamily` on roles. Satisfies DS-01, DS-02, DS-03 without changing Phase 7 components or PDF HTML.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-06-01 | Malicious font package supply chain | Low | Use official `@expo-google-fonts/*` scoped packages; pin versions in package.json; no dynamic font URLs at runtime. |
| T-06-02 | App hang on splash if fonts never resolve | Low | Always call `SplashScreen.hideAsync()` in `finally` or on error path; optional timeout wrapper documented in task. |
| T-06-03 | PII in logs | Low | Do not log font load errors with user content; dev-only logging if needed. |
</threat_model>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STITCH-SOURCE.md
@.planning/phases/06-tokens-and-typography/06-CONTEXT.md
@.planning/phases/06-tokens-and-typography/06-RESEARCH.md
@.planning/phases/06-tokens-and-typography/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google Font packages</name>
  <read_first>package.json, .planning/phases/06-tokens-and-typography/06-RESEARCH.md</read_first>
  <files>package.json, package-lock.json</files>
  <action>Add dependencies `@expo-google-fonts/manrope` and `@expo-google-fonts/inter` compatible with Expo SDK 55. Run `npm install` and ensure lockfile updates.</action>
  <acceptance_criteria>
    - `package.json` lists both packages under `dependencies`.
    - `npm ls @expo-google-fonts/manrope @expo-google-fonts/inter` succeeds.
  </acceptance_criteria>
  <verify><automated>npm install</automated></verify>
  <done>Font packages installed.</done>
</task>

<task type="auto">
  <name>Task 2: Semantic palette + legacy aliases in tokens.ts</name>
  <read_first>src/lib/theme/tokens.ts, .planning/phases/06-tokens-and-typography/06-UI-SPEC.md, .planning/research/STITCH-SOURCE.md</read_first>
  <files>src/lib/theme/tokens.ts</files>
  <action>Introduce a `semantic` (or equivalent) const object with Stitch hex values from 06-UI-SPEC. Export **`colors`** such that existing keys (`dominant`, `secondary`, `accent`, `textPrimary`, `textSecondary`, `divider`, `placeholderTint`, interpretation colors, `destructive`) remain and map to semantic tokens where specified: e.g. `dominant` → surface, `secondary` → surfaceContainerLowest, `accent` → primary, `textPrimary` → onSurface, `textSecondary` → onSurfaceVariant. Add new keys **`primary`**, **`primaryContainer`**, and any semantic keys needed by Phase 7 (`surfaceContainerLow`, `surfaceContainerHigh`, `outline`, `outlineVariant`) — either nested under `colors` or as a separate `semanticColors` export re-exported from `index.ts` (planner: pick one clear pattern, document in file comment). Update **`divider`** / **`placeholderTint`** to harmonize with outlineVariant / surface containers without breaking list readability. Tune interpretation hex only if contrast on new surfaces is insufficient.</action>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes.
    - No removal of `colors` keys used elsewhere without updating call sites in the same task batch (prefer aliases to avoid churn).
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Color tokens aligned; legacy API preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Radius pill + Stitch shadow</name>
  <read_first>src/lib/theme/tokens.ts, 06-UI-SPEC.md</read_first>
  <files>src/lib/theme/tokens.ts</files>
  <action>Add `radius.pill` = `9999`. Replace `shadow.card` with Stitch-style values: shadowColor `#1A1C1F`, lower opacity (~0.06–0.08), larger `shadowRadius` (~24–32), small `shadowOffset.height` (~4–8); set Android `elevation` to a modest value that matches (e.g. 2–4, tune visually).</action>
  <acceptance_criteria>
    - `shadow.card` documents tint rationale in a one-line comment.
    - `radius.pill` exists.
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Radius and shadow tokens updated.</done>
</task>

<task type="auto">
  <name>Task 4: Typography fontFamily + optional display letterSpacing</name>
  <read_first>src/lib/theme/tokens.ts</read_first>
  <files>src/lib/theme/tokens.ts</files>
  <action>Add `fontFamily` string to each `typography` role: display + heading → Manrope (match loaded face names from `@expo-google-fonts/manrope`, e.g. `Manrope_600SemiBold`); body + label → Inter (`Inter_400Regular`, `Inter_600SemiBold` for label). Apply optional `letterSpacing: -0.5` on `display` only. Use exact internal names Expo assigns (verify against package typings or font load map).</action>
  <acceptance_criteria>
    - All four roles include `fontFamily`.
    - `tsc` passes.
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Typography tokens reference loaded families.</done>
</task>

<task type="auto">
  <name>Task 5: Root layout font loading + splash</name>
  <read_first>src/app/_layout.tsx, 06-RESEARCH.md, expo-splash-screen docs</read_first>
  <files>src/app/_layout.tsx</files>
  <action>Call `SplashScreen.preventAutoHideAsync()` before font load (module scope or `useEffect` once). Use `useFonts` from `expo-font` with all Manrope + Inter faces needed. When `loaded` is true OR after error, call `SplashScreen.hideAsync()`. Render children only when fonts ready **or** on failure fallback to system font (same tree, no blank screen deadlock). Preserve existing `SafeAreaProvider`, `ThemeProvider`, and `Stack` structure.</action>
  <acceptance_criteria>
    - App builds; cold start does not infinite-splash on font error in dev.
    - No new navigation structure.
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Fonts load at startup with safe splash handling.</done>
</task>

<task type="auto">
  <name>Task 6: Jest / RNTL stability for fonts</name>
  <read_first>src/test/setup.ts, src/features/shell/__tests__/tabs-shell.test.tsx</read_first>
  <files>src/test/setup.ts</files>
  <action>Mock `expo-font` `useFonts` to return `[true]` immediately, or mock `@expo-google-fonts/*` so tests do not load native font assets. Ensure existing test suites pass without network.</action>
  <acceptance_criteria>
    - `npm test -- --runInBand --watchAll=false` exits 0 (or document skipped suites if pre-existing flake unrelated — fix if caused by this change).
  </acceptance_criteria>
  <verify><automated>npm test -- --runInBand --watchAll=false</automated></verify>
  <done>Tests green with font mocks.</done>
</task>

</tasks>
