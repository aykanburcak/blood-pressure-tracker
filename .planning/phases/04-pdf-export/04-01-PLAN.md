---
phase: 04-pdf-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/readings-queries.ts
  - src/lib/db/readings-repository.ts
  - src/lib/db/__tests__/readings-sql.test.ts
  - src/features/export/export-window.ts
  - src/features/export/bp-report-html.ts
  - src/features/export/__tests__/export-window.test.ts
  - src/features/export/__tests__/bp-report-html.test.ts
autonomous: true
requirements:
  - EXPT-01
  - EXPT-03
must_haves:
  truths:
    - "listReadingsInRange returns rows with startMs <= measuredAt <= endMs ordered ascending by measuredAt."
    - "buildBpReportHtml includes disclaimer text, table headers, and one row per reading with SBP/DBP/pulse."
  artifacts:
    - path: src/lib/db/readings-repository.ts
      provides: "listReadingsInRange(startMs, endMs)"
    - path: src/features/export/bp-report-html.ts
      provides: "buildBpReportHtml(rows, meta) -> HTML string"
    - path: src/features/export/export-window.ts
      provides: "preset and custom date range → { startMs, endMs }"
  key_links:
    - from: bp-report-html
      to: src/lib/bp/who-classification.ts
      via: "per-row WHO label"
      pattern: "classifyBloodPressure"
---

<objective>
Add SQLite range listing for PDF source data, pure helpers for export time windows (presets + custom local dates), and a deterministic HTML report builder with Jest coverage — no Expo native modules yet.

Purpose: Unlock EXPT-01 (range-backed content) and EXPT-03 (readable report structure) with fast unit/DB tests.
Output: Green Jest for new tests; `npx tsc --noEmit` passes.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pdf-export/04-CONTEXT.md
@.planning/phases/04-pdf-export/04-RESEARCH.md
@.planning/phases/04-pdf-export/04-UI-SPEC.md
@src/lib/db/schema.ts
@src/lib/db/readings-queries.ts
@src/lib/db/readings-repository.ts
@src/lib/db/__tests__/readings-sql.test.ts
@src/lib/bp/who-classification.ts
@src/lib/bp/medical-disclaimer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: listReadingsInRange in queries and repository</name>
  <read_first>
    - src/lib/db/readings-queries.ts
    - src/lib/db/readings-repository.ts
    - src/lib/db/schema.ts
  </read_first>
  <files>src/lib/db/readings-queries.ts, src/lib/db/readings-repository.ts</files>
  <action>In `readings-queries.ts`, import `and`, `lte` from `drizzle-orm` (in addition to existing imports). Add exported function `listReadingsInRangeFromDb(db, { startMs, endMs }: { startMs: number; endMs: number }): ReadingRow[]` that selects from `readings` where `and(gte(readings.measuredAt, startMs), lte(readings.measuredAt, endMs))` and `orderBy(asc(readings.measuredAt), asc(readings.createdAt))`. In `readings-repository.ts`, add async `listReadingsInRange(startMs: number, endMs: number, databaseName?: string): Promise&lt;ReadingRow[]&gt;` that opens DB via `getReadingsDb`, calls `listReadingsInRangeFromDb`, returns result. Export the new query function for tests that use raw db handle if needed.</action>
  <acceptance_criteria>
    - `readings-queries.ts` contains the exact substring `listReadingsInRangeFromDb`.
    - `readings-queries.ts` imports `lte` and `and` from `drizzle-orm`.
    - `readings-repository.ts` contains the exact substring `listReadingsInRange`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Range query API exists.</done>
</task>

<task type="auto">
  <name>Task 2: better-sqlite3 tests for inclusive range</name>
  <read_first>
    - src/lib/db/__tests__/readings-sql.test.ts
    - src/lib/db/readings-queries.ts
  </read_first>
  <files>src/lib/db/__tests__/readings-sql.test.ts</files>
  <action>Extend `readings-sql.test.ts`: insert rows with `measuredAt` at boundaries `startMs`, `endMs`, and strictly outside below/above; call `listReadingsInRangeFromDb` (or repository `listReadingsInRange` with test DB name) and assert exactly the inclusive boundary rows are returned in ascending time order. Reuse existing migration/open pattern from the file.</action>
  <acceptance_criteria>
    - `readings-sql.test.ts` references `listReadingsInRange` or `listReadingsInRangeFromDb` at least once.
    - `npm test -- --runInBand --watchAll=false --testPathPattern=readings-sql` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=readings-sql</automated>
  </verify>
  <done>Range semantics proven in Jest.</done>
</task>

<task type="auto">
  <name>Task 3: export-window.ts presets and custom local-day bounds</name>
  <read_first>
    - .planning/phases/04-pdf-export/04-CONTEXT.md
  </read_first>
  <files>src/features/export/export-window.ts, src/features/export/__tests__/export-window.test.ts</files>
  <action>Create `src/features/export/export-window.ts` exporting: (1) `rollingPresetRange(preset: '7d' | '30d' | '90d', nowMs: number): { startMs: number; endMs: number }` where `endMs = nowMs` and `startMs = nowMs - N*86_400_000` for N in {7,30,90}. (2) `localDayBoundsRange(startDay: Date, endDay: Date): { startMs: number; endMs: number }` that sets start to **local** midnight of `startDay` and end to **local** end-of-day (23:59:59.999) of `endDay` using the **runtime local** timezone (do not add `date-fns` — use `Date` setHours). If `endMs &lt; startMs`, throw `RangeError` with message containing `Invalid range`. Add `src/features/export/__tests__/export-window.test.ts` with a fixed `nowMs` asserting 7d window duration; one test for custom two-day span covering ordering.</action>
  <acceptance_criteria>
    - `export-window.ts` exports `rollingPresetRange` and `localDayBoundsRange`.
    - `export-window.test.ts` exists and `npm test -- --runInBand --watchAll=false --testPathPattern=export-window` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=export-window</automated>
  </verify>
  <done>Export window math is testable without UI.</done>
</task>

<task type="auto">
  <name>Task 4: buildBpReportHtml + tests</name>
  <read_first>
    - src/lib/bp/medical-disclaimer.ts
    - src/lib/bp/who-classification.ts
    - src/lib/db/schema.ts
  </read_first>
  <files>src/features/export/bp-report-html.ts, src/features/export/__tests__/bp-report-html.test.ts</files>
  <action>Create `buildBpReportHtml(input: { rows: ReadingRow[]; rangeLabel: string; generatedAtMs: number }): string` in `src/features/export/bp-report-html.ts`. Include a minimal print-safe HTML document: `&lt;!DOCTYPE html&gt;`, `&lt;meta charset="utf-8"&gt;`, inline `&lt;style&gt;` with legible `body { font-family: sans-serif; }` and table borders. Body must contain: app title string **`Blood pressure report`**, the exact disclaimer substring from **`INTERPRETATION_DISCLAIMER`** in `medical-disclaimer.ts` (import that constant), `rangeLabel`, locale date string for `generatedAtMs`, summary line with reading **count**, a `&lt;table&gt;` with header row including text **`Date`**, **`Systolic`**, **`Diastolic`**, **`Pulse`**, **`Status`** (exact substrings). For each row call `classifyBloodPressure(systolic, diastolic).label` for Status. Format dates with `toLocaleString()` default locale. Pulse column shows `—` or empty when null. Add tests asserting HTML contains `INTERPRETATION_DISCLAIMER` substring, table headers, and a fixture row’s systolic value as string.</action>
  <acceptance_criteria>
    - `bp-report-html.ts` exports `buildBpReportHtml`.
    - `bp-report-html.test.ts` contains the substrings `Blood pressure report`, `Date`, `Systolic`, and imports or references `INTERPRETATION_DISCLAIMER` for the disclaimer assertion.
    - `npm test -- --runInBand --watchAll=false --testPathPattern=bp-report-html` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=bp-report-html</automated>
  </verify>
  <done>Report HTML is deterministic and tested.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| HTML builder | Must not inject unescaped user HTML; v1 rows are numeric — still format as text nodes |
| SQLite range | Bounds must use parameterized Drizzle — no string concat |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | Range query | Mitigate | Only Drizzle `and`/`gte`/`lte` |
| T-04-02 | I | Report wording | Mitigate | Import shared disclaimer constant; no paraphrase in PDF |
</threat_model>

<verification>
`npm test -- --runInBand --watchAll=false` passes for `readings-sql`, `export-window`, `bp-report-html`; `npx tsc --noEmit` passes.
</verification>

<success_criteria>
Range query, window helpers, and HTML report builder exist with automated tests covering inclusive bounds and report content.
</success_criteria>

<output>
After execution, append `04-01-SUMMARY.md` per GSD summary template when required by workflow.
</output>
