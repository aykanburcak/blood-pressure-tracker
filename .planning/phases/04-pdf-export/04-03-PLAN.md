---
phase: 04-pdf-export
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/app/_layout.tsx
  - src/app/export-report.tsx
  - src/app/(tabs)/settings.tsx
  - src/lib/theme/tokens.ts
  - src/features/export/__tests__/export-report-screen.test.tsx
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
  - EXPT-03
must_haves:
  truths:
    - "User opens Export from Settings, picks range, generates PDF, and opens Android share sheet."
    - "Generate is disabled when there are zero readings in the selected range."
  artifacts:
    - path: src/app/export-report.tsx
      provides: "Range UI + generate + share + loading/error"
    - path: src/app/(tabs)/settings.tsx
      provides: "Navigation row to export-report"
  key_links:
    - from: export-report
      to: src/features/export/generate-bp-pdf.ts
      via: "print after buildBpReportHtml + listReadingsInRange"
      pattern: "shareAsync"
---

<objective>
Ship the export stack screen wired from Settings: range selection (presets + custom dates), reading count, PDF generation, share sheet, and RNTL coverage with native modules mocked.

Purpose: Close EXPT-01, EXPT-02, EXPT-03 in UI with `04-UI-SPEC.md` fidelity.
Output: Green full Jest run; screen reachable from Settings.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-pdf-export/04-UI-SPEC.md
@.planning/phases/04-pdf-export/04-CONTEXT.md
@src/app/_layout.tsx
@src/app/(tabs)/settings.tsx
@src/app/add-reading.tsx
@src/features/export/bp-report-html.ts
@src/features/export/export-window.ts
@src/features/export/generate-bp-pdf.ts
@src/lib/db/readings-repository.ts
@src/lib/theme/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme copy for export row and screen strings</name>
  <read_first>
    - src/lib/theme/tokens.ts
    - .planning/phases/04-pdf-export/04-UI-SPEC.md
  </read_first>
  <files>src/lib/theme/tokens.ts</files>
  <action>Add to `copy` in `tokens.ts` at least: `settingsExportPdfTitle: 'Export PDF'`, `settingsExportPdfDetail: 'Create a report for your doctor'`, `exportReportScreenTitle: 'Export report'` (or reuse header title only if using Stack `options.title`), `exportGeneratePdfLabel: 'Generate PDF'`, `exportShareLabel: 'Share'`, `exportNoReadingsInRange: 'No readings in this range'`, `exportGeneratingLabel: 'Generating…'`. Use American English; keep tone calm and non-diagnostic.</action>
  <acceptance_criteria>
    - `tokens.ts` contains the exact substrings `settingsExportPdfTitle`, `exportGeneratePdfLabel`, and `exportNoReadingsInRange`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Copy tokens exist for export UI.</done>
</task>

<task type="auto">
  <name>Task 2: Stack screen export-report + Settings row</name>
  <read_first>
    - src/app/_layout.tsx
    - src/app/(tabs)/settings.tsx
    - src/components/ui/InfoRow.tsx
  </read_first>
  <files>src/app/_layout.tsx, src/app/(tabs)/settings.tsx, src/app/export-report.tsx</files>
  <action>Register `Stack.Screen name="export-report"` in `src/app/_layout.tsx` with `headerShown: true`, `title: 'Export report'` (match `04-UI-SPEC`), `presentation: 'card'`. Create `src/app/export-report.tsx` as default export placeholder (completed in Task 3 if split — otherwise implement full screen in Task 3 in same file). In `settings.tsx`, add a tappable row: use `Pressable` wrapping `InfoRow` or `InfoRow`’s existing press pattern if any — on press `router.push('/export-report')` from `expo-router`. Use `settingsExportPdfTitle` / `settingsExportPdfDetail` from `copy`. Add `testID="settings-export-pdf"` on the pressable wrapper.</action>
  <acceptance_criteria>
    - `_layout.tsx` contains the substring `export-report`.
    - `settings.tsx` contains `settings-export-pdf` and `/export-report`.
    - `export-report.tsx` exists under `src/app/`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Navigation shell exists.</done>
</task>

<task type="auto">
  <name>Task 3: Export screen logic — range, generate, share</name>
  <read_first>
    - src/app/add-reading.tsx
    - src/features/export/export-window.ts
    - src/features/export/bp-report-html.ts
    - src/features/export/generate-bp-pdf.ts
    - src/lib/db/readings-repository.ts
  </read_first>
  <files>src/app/export-report.tsx</files>
  <action>Implement `export-report.tsx` per `04-UI-SPEC`: state for preset enum `'7d'|'30d'|'90d'|'custom'`, default `'30d'`. When preset is rolling, compute `{ startMs, endMs }` via `rollingPresetRange` with `Date.now()`. For `custom`, store two `Date` states initialized to today; use `@react-native-community/datetimepicker` in `date` mode for start/end (mirror `add-reading` patterns). On mount and when range changes, call `listReadingsInRange(startMs, endMs)` and store count + rows. **Primary button** `Generate PDF`: disabled if count===0 or `generating`; on press set `generating`, build `rangeLabel` string (e.g. `Last 30 days` or `Jan 1 – Jan 31, 2026`), `buildBpReportHtml({ rows, rangeLabel, generatedAtMs: Date.now() })`, then `generateBpPdfFromHtml`, store `pdfUri` in state. **Share** button: call `Sharing.isAvailableAsync()`; if false `Alert.alert`; else `Sharing.shareAsync(pdfUri, { mimeType: 'application/pdf', dialogTitle: 'Share report' })` from `expo-sharing`. On share promise resolve, call `deletePdfFile(pdfUri)` in `finally` or after success with `.catch(() =&gt; {})` on delete only. Use `ScreenContainer`/`ScreenTitle` or stack header only — pick one consistent with `04-UI-SPEC` (header title `Export report`). `testID="screen-export-report"` on root view. Button testIDs: `export-generate-pdf`, `export-share-pdf`.</action>
  <acceptance_criteria>
    - `export-report.tsx` contains substrings `listReadingsInRange`, `buildBpReportHtml`, `generateBpPdfFromHtml`, `shareAsync`, `screen-export-report`, `export-generate-pdf`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>End-to-end export flow implemented in UI.</done>
</task>

<task type="auto">
  <name>Task 4: RNTL tests with mocks</name>
  <read_first>
    - src/features/readings/__tests__/history-tab.test.tsx
    - jest.config.js
  </read_first>
  <files>src/features/export/__tests__/export-report-screen.test.tsx</files>
  <action>Add `src/features/export/__tests__/export-report-screen.test.tsx`. At top: `jest.mock('expo-print')`, `jest.mock('expo-sharing')`, `jest.mock('expo-file-system')` if needed, and mock `listReadingsInRange` from `@/lib/db/readings-repository` to return a fixed array with one reading. Render export screen (import from `@/app/export-report` or relative path). Fire press on `export-generate-pdf`; assert mock `printToFileAsync` or the mocked `generateBpPdfFromHtml` path was invoked (spy on module). Simpler: mock `generate-bp-pdf` module to resolve `{ uri: 'file:///tmp/x.pdf' }` and assert it was called after button press. Include test that when mocked `listReadingsInRange` returns `[]`, generate button is disabled (`toBeDisabled()`).</action>
  <acceptance_criteria>
    - File exists at `src/features/export/__tests__/export-report-screen.test.tsx`.
    - `npm test -- --runInBand --watchAll=false --testPathPattern=export-report-screen` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=export-report-screen</automated>
  </verify>
  <done>Export UI has automated regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Share sheet | User chooses destination; app does not upload by itself |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | E | Accidental share | Accept | User confirmation via system picker |
| T-04-04 | D | Deleted readings | N/A | Export uses current DB snapshot only |
</threat_model>

<verification>
`npm test -- --runInBand --watchAll=false` full suite passes.
</verification>

<success_criteria>
Settings navigates to export screen; user can generate and share PDF on device; Jest covers disabled empty state and generate path.
</success_criteria>

<output>
After execution, append `04-03-SUMMARY.md` per GSD summary template when required by workflow.
</output>
