---
phase: 04-pdf-export
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - package.json
  - src/features/export/generate-bp-pdf.ts
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
must_haves:
  truths:
    - "expo-print, expo-sharing, and expo-file-system are installed with Expo-aligned versions."
    - "generateBpPdfFromHtml returns a file URI from printToFileAsync."
  artifacts:
    - path: src/features/export/generate-bp-pdf.ts
      provides: "HTML string → temporary PDF uri + optional delete"
  key_links:
    - from: generate-bp-pdf
      to: src/features/export/bp-report-html.ts
      via: "caller passes buildBpReportHtml output"
      pattern: "printToFileAsync"
---

<objective>
Add Expo native modules for PDF generation and expose a small async API that turns HTML into a cache-scoped PDF file URI, ready for sharing.

Purpose: Satisfy EXPT-01 (device-side generation) and prepare EXPT-02 (share URI) for the UI plan.
Output: `package.json` lists the three packages; TypeScript compiles; manual smoke on Android optional in UAT.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-pdf-export/04-CONTEXT.md
@.planning/phases/04-pdf-export/04-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-print, expo-sharing, expo-file-system</name>
  <read_first>
    - package.json
  </read_first>
  <files>package.json</files>
  <action>Run `npx expo install expo-print expo-sharing expo-file-system` from the project root (executes in repo). Accept Expo-resolved versions for SDK 55. Do not hand-edit versions unless the CLI fails.</action>
  <acceptance_criteria>
    - `package.json` `dependencies` contains the exact substrings `"expo-print"`, `"expo-sharing"`, and `"expo-file-system"`.
    - `npm ls expo-print expo-sharing expo-file-system` exits 0 (no UNMET PEER).
  </acceptance_criteria>
  <verify>
    <automated>npm ls expo-print expo-sharing expo-file-system</automated>
  </verify>
  <done>Native dependencies declared.</done>
</task>

<task type="auto">
  <name>Task 2: generate-bp-pdf.ts wrapper</name>
  <read_first>
    - package.json
    - src/features/export/bp-report-html.ts
  </read_first>
  <files>src/features/export/generate-bp-pdf.ts</files>
  <action>Create `src/features/export/generate-bp-pdf.ts` exporting async `generateBpPdfFromHtml(html: string): Promise&lt;{ uri: string }&gt;` that calls `Print.printToFileAsync({ html })` from `expo-print` and returns `{ uri }` from the result. Export async `deletePdfFile(uri: string): Promise&lt;void&gt;` that calls `FileSystem.deleteAsync(uri, { idempotent: true })` from `expo-file-system`. Do not log HTML content. Add JSDoc noting Android-only product context.</action>
  <acceptance_criteria>
    - `generate-bp-pdf.ts` contains the substrings `printToFileAsync`, `deleteAsync`, and `generateBpPdfFromHtml`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PDF generation API exists.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Cache PDF URI | Residual file until deleted; user may abandon share sheet |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | E | Temp PDF on disk | Mitigate | UI plan deletes after successful share; cacheDirectory only |
| T-04-03 | I | Print HTML | Mitigate | HTML built only from app code + DB numerics in prior plan |
</threat_model>

<verification>
`npx tsc --noEmit` passes; dependencies install cleanly.
</verification>

<success_criteria>
Expo PDF and filesystem modules are integrated with a typed wrapper suitable for Settings export flow.
</success_criteria>

<output>
After execution, append `04-02-SUMMARY.md` per GSD summary template when required by workflow.
</output>
