---
phase: 09-home-pressure-trends-card
plan: 01
type: execute
wave: 1
depends_on:
  - phase-08-shell-and-screen-polish
files_modified:
  - src/lib/theme/tokens.ts
  - src/lib/bp/bp-chart-bands.ts
  - src/lib/bp/__tests__/bp-chart-bands.test.ts
  - src/features/trends/bp-chart-data.ts
  - src/features/trends/BpHomeBarChart.tsx
  - src/app/(tabs)/index.tsx
  - src/app/(tabs)/__tests__/home-latest-reading.test.tsx
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
autonomous: true
---

<objective>
Ship HOME-01–HOME-06: one Home `SurfaceCard` with section title, rounded average sys/dia (same 90-day window as `listReadingsForChart`), WHO chip on averages, `INTERPRETATION_DISCLAIMER`, and a Home-only **bar** chart with **per-reading** colors from the extended BP chart (`.planning/REQUIREMENTS.md` appendix). Remove the duplicate hero + second trend card. Keep `BpTrendChart` line chart for History. Primary CTA below the combined card.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-09-01 | Wrong band → misleading bar color | Medium | Unit tests for boundary values; map appendix rows in one module; code comment links to REQUIREMENTS appendix. |
| T-09-02 | Low contrast blue bars on white card | Low | Pick hypo blues ≥4.5:1 against white for large fills; spot-check in manual UAT. |
| T-09-03 | Empty vs sparse confused | Low | `buildBpChartSeries` unchanged; single card shows `EmptyStatePanel` when chart rows length 0; skeleton when 1 row in window. |
</threat_model>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-home-pressure-trends-card/09-CONTEXT.md
@.planning/phases/09-home-pressure-trends-card/09-UI-SPEC.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy strings for combined card</name>
  <read_first>src/lib/theme/tokens.ts (export copy block)</read_first>
  <files>src/lib/theme/tokens.ts</files>
  <action>Add to **`copy`**: **`pressureTrendsSectionTitle: 'Pressure trends'`** (display in ALL CAPS via `style` using `textTransform: 'uppercase'` on Home, not in the string), **`pressureTrendsAvgLabel: 'Avg'`**. Optionally shorten **`trendPreviewHint`** is unchanged — still used inside card for sparse state.</action>
  <acceptance_criteria>`tokens.ts` contains **`pressureTrendsSectionTitle`** and **`pressureTrendsAvgLabel`** inside **`export const copy`**; `npx tsc --noEmit` passes.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Copy keys exist.</done>
</task>

<task type="auto">
  <name>Task 2: Semantic chart band colors</name>
  <read_first>src/lib/theme/tokens.ts (`semantic` object, `colors` export)</read_first>
  <files>src/lib/theme/tokens.ts</files>
  <action>Extend **`semantic`** with keys from **09-UI-SPEC.md** table: at minimum **`chartBandNormal`** (e.g. `#22C55E`), **`chartBandPreHypertension`** (`#EAB308`), **`chartBandStage1`** (`#F97316`), **`chartBandStage2`** (`#EA580C`), **`chartBandStage3`** (`#C2410C`), **`chartBandStage4`** (`#9A3412`), **`chartBandCrisis`** (`#B91C1C`), **`chartBandHypoModerate`** (`#7DD3FC`), **`chartBandHypoSevere`** (`#38BDF8`), **`chartBandHypoExtreme`** (`#0369A1`). Spread these into **`colors`** export so **`colors.chartBandNormal`** works alongside existing **`interpretNormal`** etc.</action>
  <acceptance_criteria>`tokens.ts` defines all 10 **`chartBand*`** keys on **`semantic`** and they appear on **`colors`**; `npx tsc --noEmit` passes.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Chart tokens available.</done>
</task>

<task type="auto">
  <name>Task 3: bp-chart-bands classifier + tests</name>
  <read_first>.planning/REQUIREMENTS.md (Appendix: Blood Pressure Chart bands), 09-CONTEXT.md (D-08–D-10)</read_first>
  <files>src/lib/bp/bp-chart-bands.ts, src/lib/bp/__tests__/bp-chart-bands.test.ts</files>
  <action>Create **`src/lib/bp/bp-chart-bands.ts`** exporting at least: **`export type BpChartBandId`** (string union matching token suffixes), **`export function getBpChartBand(systolic: number, diastolic: number): BpChartBandId`**, **`export function getBpChartBarColor(systolic: number, diastolic: number): string`** returning **`colors[...]`** from theme (import **`colors`** from `@/lib/theme`). **Classification rules:** (1) **Crisis** if **`systolic > 210 || diastolic > 120`**. (2) **Extreme hypo** if **`systolic < 50 || diastolic < 35`**. (3) Else evaluate **ordered severity** using the appendix **paired** rows: **Stage 4** S 180–210 **and** D 110–120; **Stage 3** S 160–179 **and** D 100–109; **Stage 2** S 140–159 **and** D 90–99; **Stage 1** S 130–139 **and** D 85–89 — use **inclusive** integer bounds. (4) If no stage row matches, classify **systolic** and **diastolic** **independently** against their column ranges for the remaining bands (pre-hyper, high normal, normal, low normal, hypo rows) and take **`maxSeverity(sDiag, dDiag)`** with a documented total order worst→best: crisis &gt; stage4 &gt; … &gt; extreme hypo. (5) **Green family** (`low normal`, `normal`, `high normal`) maps to **`chartBandNormal`**. Implement `maxSeverity` as numeric ranks. Add **`src/lib/bp/__tests__/bp-chart-bands.test.ts`** with cases: **`(120, 80)` → normal green**, **`(160, 105)` → stage3 or worse (non-green)**, **`(95, 62)` → green family**, **`(220, 80)` → crisis**, **`(120, 125)` → crisis**.</action>
  <acceptance_criteria>`jest src/lib/bp/__tests__/bp-chart-bands.test.ts` exits 0; file **`bp-chart-bands.ts`** exports **`getBpChartBand`** and **`getBpChartBarColor`**.</acceptance_criteria>
  <verify><automated>npx jest src/lib/bp/__tests__/bp-chart-bands.test.ts</automated></verify>
  <done>Band logic tested.</done>
</task>

<task type="auto">
  <name>Task 4: Averages helper on chart rows</name>
  <read_first>src/features/trends/bp-chart-data.ts, src/lib/db/schema.ts (ReadingRow)</read_first>
  <files>src/features/trends/bp-chart-data.ts</files>
  <action>Add **`export function averageReadingsForChart(rows: ReadingRow[]): { avgSystolic: number; avgDiastolic: number } | null`** — returns **`null`** if **`rows.length === 0`**, else **`{ avgSystolic: sum(s)/n, avgDiastolic: sum(d)/n }`** using floating-point sums. Do **not** round inside this function (callers round for display and WHO).</action>
  <acceptance_criteria>`bp-chart-data.ts` exports **`averageReadingsForChart`**; `grep -n averageReadingsForChart src/features/trends/bp-chart-data.ts` returns a match.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Averages helper exists.</done>
</task>

<task type="auto">
  <name>Task 5: BpHomeBarChart component</name>
  <read_first>src/features/trends/BpTrendChart.tsx, node_modules/victory-native/dist/index.d.ts (Bar, BarGroup exports), 09-UI-SPEC.md</read_first>
  <files>src/features/trends/BpHomeBarChart.tsx</files>
  <action>Add **`BpHomeBarChart`** in **`src/features/trends/BpHomeBarChart.tsx`**. **Props:** **`series: Extract&lt;BpChartSeries, { kind: 'ok' }&gt;`** (or **`BpChartSeries`** but return placeholder if sparse — prefer parent only rendering when `kind==='ok'`). **Implementation:** Use **`CartesianChart`** from **`victory-native`** with **`data={series.data}`**, **`xKey="measuredAt"`**, **`yKeys={['systolic','diastolic']}`**, **`domainPadding`** similar to line chart. In the render child, draw **paired bars per index** with **per-reading color** from **`getBpChartBarColor(row.systolic, row.diastolic)`**. If **`BarGroup`** cannot assign **per-index colors**, render **custom Skia `RoundedRect` / `Path`** per bar using **`points.systolic[i]`** / **`points.diastolic[i]`** x coordinates and **`yScale`**, offsetting the two bars slightly (e.g. ±**`barWidth/4`**) so both are visible. Apply **top-only rounded corners** where supported. **testID:** **`bp-home-bar-chart`**. Reuse **`typography`/`colors`** for any placeholder text.</action>
  <acceptance_criteria>`BpHomeBarChart.tsx` exists; `grep -n bp-home-bar-chart src/features/trends/BpHomeBarChart.tsx` matches; `npx tsc --noEmit` passes.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Home bar chart component exists.</done>
</task>

<task type="auto">
  <name>Task 6: Home tab single-card layout</name>
  <read_first>src/app/(tabs)/index.tsx, src/lib/bp/who-classification.ts, src/lib/bp/interpretation-chip.ts, src/lib/bp/medical-disclaimer.ts</read_first>
  <files>src/app/(tabs)/index.tsx</files>
  <action>Refactor **`HomeTab`**: (1) **`useFocusEffect`** loads **`listReadingsForChart()`** only (remove **`getLatestReading`** dependency for layout). (2) Derive **`chartRows`** from repository result; **`chartSeries = buildBpChartSeries(chartRows)`**; **`averages = averageReadingsForChart(chartRows)`**. (3) **Single** **`SurfaceCard`** with **`testID="home-pressure-trends-card"`** containing: if **`chartRows.length === 0`**: **`EmptyStatePanel`** + skeleton + **`copy.trendPreviewHint`** (same as current sparse trend card). If **`chartRows.length >= 1`** but **`chartSeries.kind === 'sparse'`**: show **section title**, **`averages`** rounded display **`${Math.round(avgSystolic)} / ${Math.round(avgDiastolic)}`**, **`copy.pressureTrendsAvgLabel`**, **`mmHg`**, WHO **chip** from **`classifyBloodPressure(Math.round(avgSystolic), Math.round(avgDiastolic))`**, skeleton + hint, **disclaimer**. If **`chartSeries.kind === 'ok'`**: same header + chip + **`BpHomeBarChart series={chartSeries}`** + disclaimer. (4) Below card: **`PrimaryButton`**. (5) Keep **`copy.homeLocalCardBody`** footnote. Remove second **`SurfaceCard`** and old hero-only layout. **Do not** import or render **`BpTrendChart`** on Home.</action>
  <acceptance_criteria>`index.tsx` contains **`home-pressure-trends-card`** testID and **`BpHomeBarChart`** import; **`grep BpTrendChart src/app/(tabs)/index.tsx`** returns no match; `npx tsc --noEmit` passes.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Home merged layout.</done>
</task>

<task type="auto">
  <name>Task 7: Home integration tests</name>
  <read_first>src/app/(tabs)/__tests__/home-latest-reading.test.tsx</read_first>
  <files>src/app/(tabs)/__tests__/home-latest-reading.test.tsx</files>
  <action>Remove **`getLatestReading`** mock — mock only **`listReadingsForChart`**. **Empty test:** expect **`No readings yet`** and trend hint; **`mockListReadingsForChart.mockResolvedValue([])`**. **Single-reading test:** mock **one** row (`118/75`); expect average **`118 / 75`**, **`Normal`**, disclaimer, **`copy.trendPreviewHint`**, **no** `bp-home-bar-chart`. **Two-reading test:** mock rows **`(120,80)`** and **`(116,74)`** (means **118 / 77**); expect **`bp-home-bar-chart`** testID; **remove** expectation for string **`Blood pressure over time`**; assert **`home-pressure-trends-card`** present.</action>
  <acceptance_criteria>`npx jest "src/app/(tabs)/__tests__/home-latest-reading.test.tsx"` exits 0.</acceptance_criteria>
  <verify><automated>npx jest "src/app/(tabs)/__tests__/home-latest-reading.test.tsx"</automated></verify>
  <done>Tests updated.</done>
</task>

</tasks>

<verification>
<automated>
npx tsc --noEmit
npx jest src/lib/bp/__tests__/bp-chart-bands.test.ts
npx jest "src/app/(tabs)/__tests__/home-latest-reading.test.tsx"
</automated>
<manual>
With ≥2 readings in last 90 days: Home shows one card, bar colors change with BP level, disclaimer visible. History tab still shows line chart.
</manual>
</verification>

<must_haves>
- HOME-01: Only one primary data card on Home for BP summary + chart path.
- HOME-02: Displayed average matches mean of `listReadingsForChart` rows (rounded for UI).
- HOME-03: Home uses bar visualization (`BpHomeBarChart`); History unchanged.
- HOME-04: Bar fill driven by `getBpChartBarColor` / appendix logic.
- HOME-05: Empty and single-reading states remain understandable inside the same card.
- HOME-06: `INTERPRETATION_DISCLAIMER` still rendered on Home when data shows.
</must_haves>

## PLANNING COMPLETE
