---
phase: 03-history-and-trends
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/_layout.tsx
  - src/app/edit-reading/[id].tsx
  - src/app/(tabs)/history.tsx
  - src/features/readings/EditReadingScreen.tsx
  - src/features/readings/HistoryReadingRow.tsx
  - src/features/readings/__tests__/history-tab.test.tsx
  - src/features/readings/__tests__/edit-reading-screen.test.tsx
  - src/test/setup.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
must_haves:
  truths:
    - "History tab lists readings newest-first when data exists."
    - "Tapping a row opens edit screen for that id."
    - "Save changes calls updateReading only after Zod success; Delete shows Alert then deleteReading."
  artifacts:
    - path: src/app/(tabs)/history.tsx
      provides: "History list + navigation"
    - path: src/app/edit-reading/[id].tsx
      provides: "Routed edit entry"
    - path: src/features/readings/EditReadingScreen.tsx
      provides: "Form + save + delete"
  key_links:
    - from: EditReadingScreen
      to: src/lib/db/readings-repository.ts
      via: "getReadingById on mount; updateReading; deleteReading"
      pattern: "Reading"
---

<objective>
Ship History tab list (HIST-01), stack-based edit screen with Zod-gated save and confirmed delete (HIST-02, HIST-03), plus Jest coverage with repository mocks extended for CI.

Purpose: Complete the history loop on top of Plan 03-01 APIs.
Output: RNTL tests for History and Edit screens; `npm test` green.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-history-and-trends/03-CONTEXT.md
@.planning/phases/03-history-and-trends/03-UI-SPEC.md
@src/app/_layout.tsx
@src/app/add-reading.tsx
@src/features/readings/AddReadingScreen.tsx
@src/lib/bp/reading-schema.ts
@src/lib/db/readings-repository.ts
@src/test/setup.ts
@src/test/render.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register edit-reading stack screen</name>
  <read_first>
    - src/app/_layout.tsx
    - src/app/add-reading.tsx
  </read_first>
  <files>src/app/_layout.tsx, src/app/edit-reading/[id].tsx</files>
  <action>Add `src/app/edit-reading/[id].tsx` that default-exports a thin wrapper rendering `EditReadingScreen` from `@/features/readings/EditReadingScreen` (or inline re-export pattern matching `add-reading.tsx`). Register `Stack.Screen` with `name="edit-reading/[id]"` or the Expo Router file convention equivalent, `headerShown: true`, `title: 'Edit reading'`, `presentation: 'card'`.</action>
  <acceptance_criteria>
    - File exists at path `src/app/edit-reading/[id].tsx`.
    - `_layout.tsx` contains the literal substring `edit-reading`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Edit route is addressable from the stack.</done>
</task>

<task type="auto">
  <name>Task 2: EditReadingScreen — load, save, delete</name>
  <read_first>
    - src/features/readings/AddReadingScreen.tsx
    - src/lib/bp/reading-schema.ts
    - src/lib/db/readings-repository.ts
  </read_first>
  <files>src/features/readings/EditReadingScreen.tsx</files>
  <action>Implement `EditReadingScreen` accepting `readingId: string` prop (from route params). On mount call `getReadingById(readingId)`; if null show UI-SPEC not-found copy and back. Pre-fill systolic/diastolic/pulse/measuredAt from row. Reuse `readingInputSchema.safeParse` before `updateReading(readingId, { systolic, diastolic, pulse, measuredAt })` with `measuredAt` as epoch ms from Date state. On success `router.back()`. Add destructive `Delete reading` button calling `Alert.alert('Delete this reading?', 'This cannot be undone.', [{ text: 'Cancel', style: 'cancel' }, { text: 'Delete', style: 'destructive', onPress: async () => { await deleteReading(readingId); router.back(); } }])`. Mirror AddReadingScreen DateTimePicker and error display patterns.</action>
  <acceptance_criteria>
    - `EditReadingScreen.tsx` imports `readingInputSchema` from `@/lib/bp/reading-schema`.
    - `EditReadingScreen.tsx` imports `getReadingById`, `updateReading`, `deleteReading` from `@/lib/db/readings-repository`.
    - `EditReadingScreen.tsx` contains the literal substring `Alert.alert`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Edit and delete behaviors implemented.</done>
</task>

<task type="auto">
  <name>Task 3: History tab list and navigation</name>
  <read_first>
    - src/app/(tabs)/history.tsx
    - .planning/phases/03-history-and-trends/03-UI-SPEC.md
  </read_first>
  <files>src/app/(tabs)/history.tsx, src/features/readings/HistoryReadingRow.tsx</files>
  <action>Replace empty-only History body with `useFocusEffect` + `useCallback` reloading `listReadings()` into state. If array length 0 keep existing `EmptyStatePanel` in card. Otherwise render `FlatList` with `data={readings}`, `keyExtractor={(item) => item.id}`, `renderItem` using new `HistoryReadingRow` presenting SBP/DBP, formatted `measuredAt`, optional pulse, optional WHO label via `classifyBloodPressure` from `who-classification.ts`. `onPress` calls `router.push(\`/edit-reading/${item.id}\`)` (use template with item.id). Import `router` from `expo-router`.</action>
  <acceptance_criteria>
    - `history.tsx` contains the literal substrings `listReadings`, `FlatList`, and `useFocusEffect`.
    - `HistoryReadingRow.tsx` exists under `src/features/readings/`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>HIST-01 navigation wired.</done>
</task>

<task type="auto">
  <name>Task 4: Extend Jest readings-repository mock</name>
  <read_first>
    - src/test/setup.ts
  </read_first>
  <files>src/test/setup.ts</files>
  <action>Extend `jest.mock('@/lib/db/readings-repository')` to export `listReadings: jest.fn().mockResolvedValue([])`, `getReadingById: jest.fn().mockResolvedValue(null)`, `updateReading: jest.fn().mockResolvedValue(undefined)`, `deleteReading: jest.fn().mockResolvedValue(undefined)`, `listReadingsForChart: jest.fn().mockResolvedValue([])`, `getTrendWindowStats: jest.fn().mockResolvedValue({ last7: { count: 0, avgSystolic: null, avgDiastolic: null }, last30: { count: 0, avgSystolic: null, avgDiastolic: null } })` (adjust exact shape to match implementation from 03-01). Keep existing `getLatestReading` and `insertReading` mocks.</action>
  <acceptance_criteria>
    - `setup.ts` mock object includes the literal substrings `listReadings` and `getReadingById`.
    - `npm test -- --runInBand --watchAll=false` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false</automated>
  </verify>
  <done>Global mock matches expanded repository surface.</done>
</task>

<task type="auto">
  <name>Task 5: RNTL tests for History and Edit</name>
  <read_first>
    - src/features/readings/__tests__/add-reading-screen.test.tsx
    - src/test/render.tsx
  </read_first>
  <files>src/features/readings/__tests__/history-tab.test.tsx, src/features/readings/__tests__/edit-reading-screen.test.tsx</files>
  <action>Add tests that: (1) With mocked `listReadings` returning two rows with distinct ids, render History tab component tree and assert both SBP/DBP strings appear and highest measuredAt row appears first (or assert order by query order from mock). (2) Edit screen: mock `getReadingById` to return a row; assert fields populated; fire save with `updateReading` called with expected integers after valid input. (3) Delete: mock Alert — use `jest.spyOn` on `Alert.alert` from `react-native` and invoke destructive callback to assert `deleteReading` called with id. Use `@testing-library/react-native` patterns consistent with existing tests.</action>
  <acceptance_criteria>
    - Files `history-tab.test.tsx` and `edit-reading-screen.test.tsx` exist under `src/features/readings/__tests__/`.
    - `npm test -- --runInBand --watchAll=false --testPathPattern="history-tab|edit-reading"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern="history-tab|edit-reading"</automated>
  </verify>
  <done>Automated coverage for HIST-* UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edit screen | Must not write invalid numbers to SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T | update payload | mitigate | Zod same as add; acceptance grep `readingInputSchema` in EditReadingScreen |
| T-03-04 | E | delete | mitigate | Alert confirmation required before deleteReading |
</threat_model>

<verification>
`npm test -- --runInBand --watchAll=false` full suite green.
</verification>

<success_criteria>
History list, edit route, and delete confirmation work; tests cover primary paths.
</success_criteria>

<output>
After execution, append `03-02-SUMMARY.md` per GSD summary template when required.
</output>
