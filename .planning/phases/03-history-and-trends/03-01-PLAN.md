---
phase: 03-history-and-trends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/readings-repository.ts
  - src/lib/db/__tests__/readings-sql.test.ts
autonomous: true
requirements:
  - HIST-02
  - HIST-03
  - TRND-02
  - TRND-03
must_haves:
  truths:
    - "listReadings returns rows ordered by measuredAt desc then createdAt desc."
    - "updateReading and deleteReading target a single id; getReadingById returns null for unknown id."
    - "Trend window stats expose 7d and 30d avg systolic/diastolic and counts from SQLite."
    - "Chart source query returns rows sorted ascending by measuredAt for paired SBP/DBP series."
  artifacts:
    - path: src/lib/db/readings-repository.ts
      provides: "list, get, update, delete, range list, trend stats"
    - path: src/lib/db/__tests__/readings-sql.test.ts
      provides: "better-sqlite3 proof of ordering, CRUD, aggregates"
  key_links:
    - from: readings-repository
      to: src/lib/db/schema.ts
      via: "readings table columns unchanged in Phase 3"
      pattern: "readings."
---

<objective>
Extend the readings repository with list, get-by-id, update, delete, time-window listing for charts, and rolling 7-day / 30-day aggregate stats — all covered by existing better-sqlite3 migration tests.

Purpose: Give History and Trends UI plans typed, testable data access without new schema columns (D-33).
Output: Green Jest for `readings-sql.test.ts`; `readings-repository.ts` exports new async functions.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-history-and-trends/03-CONTEXT.md
@.planning/phases/03-history-and-trends/03-RESEARCH.md
@.planning/phases/03-history-and-trends/03-VALIDATION.md
@src/lib/db/schema.ts
@src/lib/db/readings-repository.ts
@src/lib/db/__tests__/readings-sql.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: listReadings and getReadingById</name>
  <read_first>
    - src/lib/db/readings-repository.ts
    - src/lib/db/schema.ts
    - src/lib/db/__tests__/readings-sql.test.ts
  </read_first>
  <files>src/lib/db/readings-repository.ts</files>
  <action>Add async `listReadings(databaseName?: string): Promise&lt;ReadingRow[]&gt;` selecting all columns from `readings`, ordered by `desc(readings.measuredAt)`, then `desc(readings.createdAt)`. Add async `getReadingById(id: string, databaseName?: string): Promise&lt;ReadingRow | null&gt;` using `db.select().from(readings).where(eq(readings.id, id)).get()` (or equivalent Drizzle API). Use the same `getReadingsDb` + migration pattern as existing functions. Do not use raw string SQL concatenation with user `id` — use Drizzle `eq`.</action>
  <acceptance_criteria>
    - `readings-repository.ts` contains the exact substrings `listReadings` and `getReadingById`.
    - `readings-repository.ts` imports `eq` from `drizzle-orm` if `where(eq(...))` is used.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>List and get-by-id APIs exist.</done>
</task>

<task type="auto">
  <name>Task 2: updateReading and deleteReading</name>
  <read_first>
    - src/lib/db/readings-repository.ts
    - src/lib/db/schema.ts
  </read_first>
  <files>src/lib/db/readings-repository.ts</files>
  <action>Add `updateReading(id: string, values: InsertReadingValues, databaseName?: string): Promise&lt;void&gt;` using Drizzle `update(readings).set({ systolic, diastolic, pulse, measuredAt })` with `where(eq(readings.id, id))` and `.run()`. Add `deleteReading(id: string, databaseName?: string): Promise&lt;void&gt;` using `delete(readings).where(eq(readings.id, id)).run()`. Do not add soft-delete columns. Import `and` only if needed.</action>
  <acceptance_criteria>
    - `readings-repository.ts` contains the exact substrings `updateReading` and `deleteReading`.
    - `readings-repository.ts` uses `update(` and `delete(` from Drizzle query API on `readings`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Mutations available for edit and delete flows.</done>
</task>

<task type="auto">
  <name>Task 3: listReadingsForChart and trend window stats</name>
  <read_first>
    - src/lib/db/readings-repository.ts
    - .planning/phases/03-history-and-trends/03-CONTEXT.md
  </read_first>
  <files>src/lib/db/readings-repository.ts</files>
  <action>Add `listReadingsForChart(databaseName?: string, options?: { startMs?: number }): Promise&lt;ReadingRow[]&gt;` returning rows with `measuredAt &gt;= (options?.startMs ?? Date.now() - 90*86400000)` ordered by **asc**(`readings.measuredAt`) then **asc**(`readings.createdAt`) for stable chart x-axis. Add `getTrendWindowStats(nowMs: number, databaseName?: string)` returning an object with shape `{ last7: { count, avgSystolic, avgDiastolic }, last30: { count, avgSystolic, avgDiastolic } }` where averages are **null** when count is 0 (TypeScript optional or null). Use `sql\`avg(${readings.systolic})\`` / Drizzle avg helpers or select raw with `db.all` — prefer typed Drizzle `avg()` from `drizzle-orm` with `where(and(gte(measuredAt, cutoff), ...))`. Windows: `nowMs - 7*86400000` and `nowMs - 30*86400000`.</action>
  <acceptance_criteria>
    - `readings-repository.ts` contains the exact substrings `listReadingsForChart` and `getTrendWindowStats`.
    - `readings-repository.ts` imports `asc` from `drizzle-orm` if ascending order is used.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Chart and summary queries implemented.</done>
</task>

<task type="auto">
  <name>Task 4: better-sqlite3 tests for new repository behaviors</name>
  <read_first>
    - src/lib/db/__tests__/readings-sql.test.ts
    - src/lib/db/readings-repository.ts
  </read_first>
  <files>src/lib/db/__tests__/readings-sql.test.ts</files>
  <action>Extend `readings-sql.test.ts` to: (1) insert at least three rows with distinct `measuredAt` and assert `listReadings` order is newest-first; (2) assert `getReadingById` returns a row and unknown id returns null; (3) `updateReading` changes values and `deleteReading` removes row; (4) assert `listReadingsForChart` returns ascending time order; (5) seed rows inside/outside 7d window and assert `getTrendWindowStats` counts and averages match expected integers. Use a unique temp DB file name per test describe block if needed to avoid flakiness. Reuse existing migration application pattern from the file.</action>
  <acceptance_criteria>
    - `readings-sql.test.ts` references `listReadings`, `getReadingById`, `updateReading`, `deleteReading`, `listReadingsForChart`, and `getTrendWindowStats` at least once each (grep).
    - `npm test -- --runInBand --watchAll=false --testPathPattern=readings-sql` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=readings-sql</automated>
  </verify>
  <done>Data layer verified without device.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → SQLite | SQL injection if ids or filters are concatenated unsafely |
| Tests → DB file | Tests must use isolated DB names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | readings-repository mutations | mitigate | Drizzle `eq` / builders only; grep forbids template SQL with `${id}` |
| T-03-02 | E | delete | mitigate | UI plan gates delete with Alert; repository performs hard delete only when called |
</threat_model>

<verification>
`npm test -- --runInBand --watchAll=false` passes including `readings-sql.test.ts`.
</verification>

<success_criteria>
All Phase 3 repository methods exist, are typed, and are proven by better-sqlite3 tests.
</success_criteria>

<output>
After execution, append `03-01-SUMMARY.md` per GSD summary template when required by workflow.
</output>
