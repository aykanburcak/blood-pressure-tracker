---
phase: 03-history-and-trends
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - package.json
  - src/features/trends/bp-chart-data.ts
  - src/features/trends/__tests__/bp-chart-data.test.ts
  - src/features/trends/BpTrendChart.tsx
  - src/features/trends/TrendSummaryStrip.tsx
  - src/app/(tabs)/history.tsx
  - src/features/readings/__tests__/history-trends.test.tsx
  - jest.config.js
  - src/test/setup.ts
autonomous: true
requirements:
  - TRND-01
  - TRND-02
  - TRND-03
must_haves:
  truths:
    - "History shows 7d/30d summary numbers from getTrendWindowStats (not hard-coded)."
    - "Chart plots systolic and diastolic from the same ReadingRow sequence (paired x)."
    - "Sparse data shows UI-SPEC placeholder instead of empty crash."
  artifacts:
    - path: src/features/trends/BpTrendChart.tsx
      provides: "Skia/Victory dual-line chart"
    - path: src/features/trends/TrendSummaryStrip.tsx
      provides: "7d/30d averages row"
  key_links:
    - from: history.tsx
      to: src/lib/db/readings-repository.ts
      via: "listReadingsForChart + getTrendWindowStats on focus"
      pattern: "Trend"
---

<objective>
Add Skia-backed dual-series timeline chart and rolling-window summary strip to the History tab, driven by Plan 03-01 queries, with Jest-stable tests for data shaping and header content.

Purpose: Close TRND-01, TRND-02, TRND-03 on-device; keep CI green via mocks/pure tests.
Output: New `src/features/trends/*`; `package.json` includes skia + victory-native; dev build note in SUMMARY.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-history-and-trends/03-CONTEXT.md
@.planning/phases/03-history-and-trends/03-UI-SPEC.md
@.planning/phases/03-history-and-trends/03-RESEARCH.md
@src/app/(tabs)/history.tsx
@src/lib/db/readings-repository.ts
@src/lib/theme/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Skia and Victory Native (Expo-compatible)</name>
  <read_first>
    - package.json
    - AGENTS.md
  </read_first>
  <files>package.json</files>
  <action>Run `npx expo install @shopify/react-native-skia victory-native` from project root (adds compatible versions for Expo SDK 55). Do not remove `react-native-reanimated`. If `victory-native` resolves to a meta-package that re-exports XL, keep lockfile consistent with one install command. Document in `03-03-SUMMARY.md` that **Expo Go may not load Skia** and a **development build** is required for manual chart verification.</action>
  <acceptance_criteria>
    - `package.json` `dependencies` contains the literal substring `@shopify/react-native-skia`.
    - `package.json` `dependencies` contains the literal substring `victory-native`.
    - `npm install` exits 0 (executor runs after edit if needed).
  </acceptance_criteria>
  <verify>
    <automated>npm install</automated>
  </verify>
  <done>Native chart dependencies declared.</done>
</task>

<task type="auto">
  <name>Task 2: Pure chart data builder + unit tests (TRND-03)</name>
  <read_first>
    - src/lib/db/schema.ts
  </read_first>
  <files>src/features/trends/bp-chart-data.ts, src/features/trends/__tests__/bp-chart-data.test.ts</files>
  <action>Create `src/features/trends/bp-chart-data.ts` exporting `buildBpChartSeries(rows: ReadingRow[])` returning `{ xValues: number[]; systolic: number[]; diastolic: number[] }` where each index `i` maps one reading: `xValues[i]` is `measuredAt` (number ms), `systolic[i]` and `diastolic[i]` from the same row. Require `rows` already sorted ascending by time (caller responsibility). If row count is 0 or 1, return a discriminated result `{ kind: 'sparse' }` vs `{ kind: 'ok', ...series }` for UI branching. Add Jest tests: for sorted input of length 3, assert equal lengths and systolic[i]/diastolic[i] match row i; test sparse branch when 0–1 rows.</action>
  <acceptance_criteria>
    - `bp-chart-data.ts` contains the substring `buildBpChartSeries`.
    - Test file asserts same-length arrays for systolic/diastolic/xValues on `kind === 'ok'`.
    - `npm test -- --runInBand --watchAll=false --testPathPattern=bp-chart-data` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=bp-chart-data</automated>
  </verify>
  <done>Paired series logic tested without Skia.</done>
</task>

<task type="auto">
  <name>Task 3: BpTrendChart and TrendSummaryStrip components</name>
  <read_first>
    - .planning/phases/03-history-and-trends/03-UI-SPEC.md
    - src/lib/theme/tokens.ts
    - src/components/ui/SurfaceCard.tsx
  </read_first>
  <files>src/features/trends/BpTrendChart.tsx, src/features/trends/TrendSummaryStrip.tsx</files>
  <action>Implement `TrendSummaryStrip` with a `stats` prop typed from `getTrendWindowStats` return type (import from readings-repository). Render last7 and last30 with averages as integers or one decimal and counts; show em dash U+2014 or hyphen for null averages per UI-SPEC. Implement `BpTrendChart` accepting the discriminated output of `buildBpChartSeries`: sparse branch renders UI-SPEC placeholder inside `SurfaceCard`; ok branch renders Victory Native chart with two lines (systolic vs diastolic). Use theme colors distinct from interpretation crisis red. Chart height approximately 200 to 240 logical pixels.</action>
  <acceptance_criteria>
    - `BpTrendChart.tsx` imports from `victory-native` (grep `victory-native`).
    - `BpTrendChart.tsx` contains the literal substring `sparse` OR imports sparse kind from chart data module for placeholder branch.
    - `TrendSummaryStrip.tsx` contains the literal substring `last7` or `Last 7` (UI copy).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Trend UI building blocks exist.</done>
</task>

<task type="auto">
  <name>Task 4: Wire trends into History tab focus load</name>
  <read_first>
    - src/app/(tabs)/history.tsx
    - src/lib/db/readings-repository.ts
  </read_first>
  <files>src/app/(tabs)/history.tsx</files>
  <action>In `history.tsx`, extend the same `useFocusEffect` reload to also `await getTrendWindowStats(Date.now())` and `await listReadingsForChart()` (use default 90d window from repository). Pass stats to `TrendSummaryStrip`. Pass chart rows through `buildBpChartSeries` into `BpTrendChart`. Compose with `FlatList` using `ListHeaderComponent` rendering summary strip + chart card above the list (UI-SPEC order). Ensure loading state does not flash infinite spinner — optional brief `useState` loading bool agent discretion.</action>
  <acceptance_criteria>
    - `history.tsx` contains the literal substrings `getTrendWindowStats` and `listReadingsForChart`.
    - `history.tsx` imports `BpTrendChart` and `TrendSummaryStrip` from `@/features/trends/` paths (or relative paths under `src/features/trends`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TRND-01/02 wired on History.</done>
</task>

<task type="auto">
  <name>Task 5: Jest stability for Skia/Victory</name>
  <read_first>
    - jest.config.js
    - src/test/setup.ts
  </read_first>
  <files>jest.config.js, src/test/setup.ts, src/features/readings/__tests__/history-trends.test.tsx</files>
  <action>If full `npm test` fails on Skia native bindings, add `jest.mock('@shopify/react-native-skia', () => require('react-native'))` or minimal stub object in `setup.ts` **or** mock `BpTrendChart` in `history-trends.test.tsx` only. Add `history-trends.test.tsx` that renders History (or list header fragment) with mocked repository returning 2+ rows and stats object with non-zero count, then assert summary strip shows expected average substring (e.g. `120` from fixture). Goal: `npm test -- --runInBand --watchAll=false` exits 0 for entire repo.</action>
  <acceptance_criteria>
    - `npm test -- --runInBand --watchAll=false` exits 0.
    - File `history-trends.test.tsx` exists.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false</automated>
  </verify>
  <done>CI green with trend UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chart | Renders local SQLite-derived numbers only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | I | trend stats | accept | Local-only; no network exfil |
</threat_model>

<verification>
Full Jest suite green; manual dev-build spot-check on Android per `03-VALIDATION.md`.
</verification>

<success_criteria>
History tab shows summaries + chart per UI-SPEC; paired BP series from one query; tests cover data shaping and header.
</success_criteria>

<output>
After execution, append `03-03-SUMMARY.md` noting development build requirement for Skia.
</output>
