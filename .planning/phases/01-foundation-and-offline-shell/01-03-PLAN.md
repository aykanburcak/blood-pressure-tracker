---
phase: 01-foundation-and-offline-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/components/ui/ScreenContainer.tsx
  - src/components/ui/ScreenTitle.tsx
  - src/components/ui/SurfaceCard.tsx
  - src/components/ui/InfoRow.tsx
  - src/components/ui/EmptyStatePanel.tsx
  - src/components/ui/PrimaryButton.tsx
  - src/components/navigation/TabIcon.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/history.tsx
  - app/(tabs)/settings.tsx
  - src/features/shell/__tests__/tabs-shell.test.tsx
autonomous: false
requirements:
  - CORE-01
  - CORE-03
must_haves:
  truths:
    - "The Home tab already looks like a finished health dashboard even with zero readings."
    - "The History tab is a real screen with an empty list container instead of placeholder language."
    - "The Settings tab explains the local-only storage and future export boundary without implying cloud features."
  artifacts:
    - path: src/components/ui/SurfaceCard.tsx
      provides: "Reusable card surface for the Apple Health-inspired shell"
    - path: app/(tabs)/index.tsx
      provides: "Polished empty-state Home dashboard"
    - path: app/(tabs)/history.tsx
      provides: "Real History screen structure with empty-state list area"
    - path: app/(tabs)/settings.tsx
      provides: "Settings screen with privacy/storage framing"
    - path: src/features/shell/__tests__/tabs-shell.test.tsx
      provides: "Automated assertions for shell copy, tab order, and placeholder-free screens"
  key_links:
    - from: src/components/ui/ScreenContainer.tsx
      to: "all tab routes"
      via: "shared safe-area spacing and screen chrome"
      pattern: "ScreenContainer"
    - from: src/components/navigation/TabIcon.tsx
      to: app/(tabs)/_layout.tsx
      via: "Ionicons-based tab presentation"
      pattern: "Ionicons"
    - from: app/(tabs)/settings.tsx
      to: app/privacy.tsx
      via: "copy and row structure preserve the local-only/export boundary"
      pattern: "Local storage only"
---

<objective>
Finish the Phase 1 product shell by building the approved UI primitives and the polished empty-state Home, History, and Settings surfaces, then verify the result on Android.

Purpose: Land the product feel promised in the phase context instead of stopping at bare routing.
Output: Shared UI primitives, final empty-state tabs, shell tests, and a blocking human verification checkpoint.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aykanburcak/Documents/Projects/blood-pressure-tracker/AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-offline-shell/01-CONTEXT.md
@.planning/phases/01-foundation-and-offline-shell/01-RESEARCH.md
@.planning/phases/01-foundation-and-offline-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-and-offline-shell/01-VALIDATION.md
@.planning/phases/01-foundation-and-offline-shell/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared Phase 1 UI primitives and final empty-state tab surfaces</name>
  <read_first>
    - .planning/phases/01-foundation-and-offline-shell/01-CONTEXT.md
    - .planning/phases/01-foundation-and-offline-shell/01-UI-SPEC.md
    - app/(tabs)/_layout.tsx
    - app/(tabs)/index.tsx
    - app/(tabs)/history.tsx
    - app/(tabs)/settings.tsx
    - src/lib/theme/tokens.ts
  </read_first>
  <files>src/components/ui/ScreenContainer.tsx, src/components/ui/ScreenTitle.tsx, src/components/ui/SurfaceCard.tsx, src/components/ui/InfoRow.tsx, src/components/ui/EmptyStatePanel.tsx, src/components/ui/PrimaryButton.tsx, src/components/navigation/TabIcon.tsx, app/(tabs)/index.tsx, app/(tabs)/history.tsx, app/(tabs)/settings.tsx</files>
  <action>Implement the approved UI spec literally. Build the shared primitives first, then apply them to the three tab routes. Home must satisfy D-06 and D-07 with a large `Blood Pressure` title, a summary hero card anchored above the fold, a local-only supporting card, and a muted trend-preview card. History must satisfy D-10 and D-11 with a real list container and the approved `No history yet` copy, not blank space or “coming soon” messaging. Settings must present real `Local storage only` and `About this app` rows plus the export-boundary footnote from the UI spec. Keep the Apple Health-inspired white, airy card language from D-08 and D-09. Do not add sample readings, fake charts, logging inputs, edit/delete controls, or PDF actions; those belong to later phases.</action>
  <acceptance_criteria>
    - All listed UI primitive files exist and are imported by the tab routes rather than leaving layout duplicated inline.
    - `app/(tabs)/index.tsx` contains the `Blood Pressure` title, `No readings yet` hero content, local-only support copy, and a trend-preview section.
    - `app/(tabs)/history.tsx` contains the `History` title, supporting sentence, and a visible list container with the approved empty-state copy.
    - `app/(tabs)/settings.tsx` contains the `Settings` title, `Local storage only`, `About this app`, and the export-boundary footnote.
    - `rg -n "Coming soon|sample data|fake|mock reading" app/(tabs) src/components` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false src/features/shell/__tests__/tabs-shell.test.tsx</automated>
  </verify>
  <done>The Phase 1 shell looks like a finished product structure rather than an MVP placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell integration tests for the final offline surfaces</name>
  <read_first>
    - .planning/phases/01-foundation-and-offline-shell/01-VALIDATION.md
    - app/(tabs)/_layout.tsx
    - app/(tabs)/index.tsx
    - app/(tabs)/history.tsx
    - app/(tabs)/settings.tsx
    - src/test/render.tsx
  </read_first>
  <files>src/features/shell/__tests__/tabs-shell.test.tsx</files>
  <action>Write focused screen tests that prove the shell is complete enough for Phase 1. Cover tab order, screen titles, the approved empty-state copy, absence of placeholder language, and the local-only/export-boundary messaging in Settings. Assert concrete user-visible text and tab labels rather than snapshots. Reuse the render helper from Plan 01 and keep the tests offline by default.</action>
  <acceptance_criteria>
    - `src/features/shell/__tests__/tabs-shell.test.tsx` asserts all three tab titles and their main empty-state copy blocks.
    - The test file checks for absence of `Coming soon` language on History and Settings.
    - The test file verifies Settings preserves the local-only and later-export copy boundary.
    - `npm test -- --runInBand --watchAll=false` passes with both privacy-gate and tabs-shell coverage.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false</automated>
  </verify>
  <done>The final Phase 1 shell is covered by automated assertions for both routing and user-visible empty-state content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the Android shell feels finished and privacy-first</name>
  <read_first>
    - .planning/phases/01-foundation-and-offline-shell/01-UI-SPEC.md
    - .planning/phases/01-foundation-and-offline-shell/01-VALIDATION.md
    - app/privacy.tsx
    - app/(tabs)/index.tsx
    - app/(tabs)/history.tsx
    - app/(tabs)/settings.tsx
  </read_first>
  <files>none - checkpoint only after app code and automated tests from Tasks 1-2 are complete</files>
  <action>After Tasks 1 and 2 are green, launch the Android app from a clean install state and perform the manual Phase 1 review from `01-VALIDATION.md`. This checkpoint exists because D-07, D-08, and D-09 require product-quality visual judgment that Jest cannot prove. Do not approve the plan if the shell still looks like a starter template, if the privacy gate feels bolted on, or if any screen implies unbuilt cloud/account behavior.</action>
  <acceptance_criteria>
    - A clean Android launch shows the privacy gate first, then the tab shell after acknowledgement.
    - Home, History, and Settings all feel like real product surfaces with no starter-template residue.
    - The shell is calm, white, airy, and card-led, matching the approved UI contract closely enough to proceed.
    - No network prompt, auth prompt, or placeholder copy appears during the flow.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false</automated>
  </verify>
  <what-built>Shared UI primitives, final tab surfaces, and automated shell coverage for the complete Phase 1 experience.</what-built>
  <how-to-verify>
    1. Run the Android app from a clean install state.
    2. Confirm the dedicated privacy screen appears before tabs and the CTA reads `Continue Offline`.
    3. Acknowledge the privacy screen and verify the tab order is `Home`, `History`, `Settings`.
    4. On `Home`, verify the hero card, local-only support card, and trend-preview card are present and the screen does not feel empty or temporary.
    5. On `History`, verify the list container is visible even with no readings and there is no “coming soon” language.
    6. On `Settings`, verify the `Local storage only` row, `About this app` row, and the export-boundary footnote are present.
  </how-to-verify>
  <resume-signal>Type `approved` to close Phase 1 execution, or describe the visual/flow issues that must be corrected.</resume-signal>
  <done>The automated suite is green and a human reviewer has approved the Android shell as privacy-first, polished, and aligned to the Phase 1 UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved design contract -> implemented shell | UI drift can quietly weaken privacy framing or make unfinished screens look deceptive |
| tab routes -> user expectations | Empty-state screens must communicate truthful scope without placeholder language |
| settings copy -> future export/privacy boundary | Misworded copy can imply sync or diagnosis features that do not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | T | Home/History/Settings screens | mitigate | Encode the approved copy and screen structure in tests; forbid “coming soon” and fake data language in acceptance criteria |
| T-01-10 | I | Settings/privacy copy | mitigate | Use explicit local-only and optional-export wording from the UI spec so the app does not imply cloud storage or diagnosis |
| T-01-11 | D | shell composition | mitigate | Keep screens lightweight and local-only with no network-triggered placeholders or remote assets required for rendering |
| T-01-12 | R | visual readiness claim | mitigate | Require a blocking human verification step on Android before closing the phase so design/regression issues are caught before sign-off |
</threat_model>

<verification>
Run the full test suite, then complete the Android human verification steps from `01-VALIDATION.md` before marking the plan done.
</verification>

<success_criteria>
Phase 1 ends with a polished offline shell: first-run privacy framing, durable tabs, Apple Health-inspired empty states, green automated coverage, and explicit Android sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-offline-shell/01-03-SUMMARY.md`
</output>
