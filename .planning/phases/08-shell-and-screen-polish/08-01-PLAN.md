---
phase: 08-shell-and-screen-polish
plan: 01
type: execute
wave: 1
depends_on:
  - phase-07-core-components
files_modified:
  - src/app/_layout.tsx
  - src/app/(tabs)/_layout.tsx
  - src/app/(tabs)/settings.tsx
  - src/features/readings/HistoryReadingRow.tsx
  - src/features/export/bp-report-html.ts
  - src/features/export/__tests__/bp-report-html.test.ts
requirements:
  - DS-08
  - DS-09
  - DS-10
autonomous: true
---

<objective>
Deliver DS-08–DS-10: tab bar and stack header chrome aligned with Stitch tokens (solid bar, no glass); reduce divider-heavy Settings and History list UI; align PDF report HTML/CSS with semantic neutrals. No new features or copy changes.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-08-01 | Dark mode stack/tab mismatch | Low | Apply token overrides only when `useColorScheme() !== 'dark'`; keep `DarkTheme` + defaults for dark. |
| T-08-02 | History list spacing too loose | Low | Use `spacing.sm` between rows; tune if FlatList looks odd. |
| T-08-03 | PDF contrast regression | Low | Match documented Stitch hex; keep disclaimer block readable. |
</threat_model>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-shell-and-screen-polish/08-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation theme + stack headers (DS-08)</name>
  <read_first>src/app/_layout.tsx, 08-CONTEXT.md</read_first>
  <files>src/app/_layout.tsx</files>
  <action>Import **`colors`** from `@/lib/theme`. For **`colorScheme !== 'dark'`**, pass a **`ThemeProvider`** value that spreads **`DefaultTheme`** and sets **`colors.primary`**, **`background`**, **`card`**, **`text`**, **`border`** from tokens (`primary`, `surface`, `onSurface`, `outlineVariant`). Keep **`DarkTheme`** when dark. On **`Stack`**, add **`screenOptions`** (merged with existing) so when headers show: **`headerStyle.backgroundColor: colors.surface`**, **`headerTintColor: colors.primary`**, **`headerTitleStyle.color: colors.onSurface`**, **`headerShadowVisible: false`** — only for light (same guard as theme). Preserve **`headerShown: false`** default and per-screen overrides.</action>
  <acceptance_criteria>Light mode modals (add/edit/export/privacy-info) show surface header, primary back tint, on-surface title; dark unchanged.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Stack chrome matches DS-08.</done>
</task>

<task type="auto">
  <name>Task 2: Tab bar colors (DS-08)</name>
  <read_first>src/app/(tabs)/_layout.tsx</read_first>
  <files>src/app/(tabs)/_layout.tsx</files>
  <action>Set **`tabBarStyle.backgroundColor`** to **`colors.surface`** (tonal match with app canvas). Set **`tabBarActiveTintColor`** to **`colors.primary`** (may replace `colors.accent`; same hex). Keep inactive **`colors.textSecondary`**, safe-area height math, and **`shell.tabBarVisualHeight`**. Optionally add **`borderTopWidth: StyleSheet.hairlineWidth`**, **`borderTopColor: colors.outlineVariant`** only if the bar needs separation from content — agent discretion.</action>
  <acceptance_criteria>Tab bar reads as Stitch surface, not pure white card.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Tab bar matches DS-08.</done>
</task>

<task type="auto">
  <name>Task 3: Settings — remove hairline dividers (DS-09)</name>
  <read_first>src/app/(tabs)/settings.tsx</read_first>
  <files>src/app/(tabs)/settings.tsx</files>
  <action>Remove **`View` divider** elements and **`styles.divider`**. Separate **`InfoRow`** blocks with **`marginVertical`** or **`gap`** on a wrapper so Export / Local / Privacy remain visually grouped without hairlines (e.g. **`gap: spacing.md`** in column or **`marginTop`** on non-first rows).</action>
  <acceptance_criteria>No hairline rules inside Settings `SurfaceCard`; spacing-only separation.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Settings matches DS-09.</done>
</task>

<task type="auto">
  <name>Task 4: History row — no bottom border (DS-09)</name>
  <read_first>src/features/readings/HistoryReadingRow.tsx, src/app/(tabs)/history.tsx</read_first>
  <files>src/features/readings/HistoryReadingRow.tsx</files>
  <action>Remove **`borderBottomWidth`** / **`borderBottomColor`**. Add **`marginBottom: spacing.sm`** (or equivalent) on the row container so items separate by space, not rules.</action>
  <acceptance_criteria>No dividers between history rows; list still scannable.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>History list matches DS-09.</done>
</task>

<task type="auto">
  <name>Task 5: PDF HTML/CSS (DS-10)</name>
  <read_first>src/features/export/bp-report-html.ts, src/lib/theme/tokens.ts (semantic hex)</read_first>
  <files>src/features/export/bp-report-html.ts</files>
  <action>Update inline styles to Stitch-aligned literals (comment: matches `tokens` semantic): body **`#1A1C1F`**, muted **`#414755`**, table border **`#C1C6D7`**, **`th` background `#F3F3F8`**, disclaimer text use **`#414755`** or **`#1A1C1F`** as appropriate. Use **`font-family: system-ui, sans-serif`**. Remove legacy gray-900/gray-200 palette.</action>
  <acceptance_criteria>Generated HTML uses new palette only; structure unchanged.</acceptance_criteria>
  <verify><automated>npx jest src/features/export/__tests__/bp-report-html.test.ts</automated></verify>
  <done>PDF template matches DS-10.</done>
</task>

<task type="auto">
  <name>Task 6: PDF test assertions</name>
  <read_first>src/features/export/__tests__/bp-report-html.test.ts</read_first>
  <files>src/features/export/__tests__/bp-report-html.test.ts</files>
  <action>Add expectations that output contains at least one new token hex (e.g. **`#1A1C1F`**, **`#F3F3F8`**) and does **not** contain a retired literal such as **`#111827`** or **`#F9FAFB`**.</action>
  <acceptance_criteria>Test locks palette migration.</acceptance_criteria>
  <verify><automated>npx jest src/features/export/__tests__/bp-report-html.test.ts</automated></verify>
  <done>Tests guard DS-10.</done>
</task>

</tasks>

<verification>
<automated>
npx tsc --noEmit
npx jest src/features/export/__tests__/bp-report-html.test.ts
</automated>
<manual>
Light mode: open Settings (no dividers), History (spaced rows), tabs (surface bar), Add reading / Export report headers (surface + primary tint).
</manual>
</verification>
