---
phase: 05-privacy-and-release-hardening
plan: 01
type: execute
wave: 2
depends_on:
  - 05-02-PLAN.md
files_modified:
  - src/features/onboarding/privacy-copy.ts
  - src/features/privacy/privacy-detail-copy.ts
  - src/app/privacy-info.tsx
  - src/app/_layout.tsx
  - src/app/(tabs)/settings.tsx
  - src/lib/theme/tokens.ts
  - src/features/privacy/__tests__/privacy-info-screen.test.tsx
autonomous: true
requirements:
  - PRIV-02
must_haves:
  truths:
    - "User can open a scrollable Privacy & data screen from Settings with sections covering local storage, no account/sync/ads, PDF export boundary, offline core use, Android backup disabled, OS transfer caveat."
    - "First-run /privacy gate remains short and unchanged in behavior; long-form content lives on privacy-info route."
  artifacts:
    - path: src/features/privacy/privacy-detail-copy.ts
      provides: "Section titles and paragraph strings for PRIV-02"
    - path: src/app/privacy-info.tsx
      provides: "Stack screen rendering long-form privacy copy"
    - path: src/app/(tabs)/settings.tsx
      provides: "Navigates to privacy-info from Privacy or About row"
  key_links:
    - from: privacy-detail-copy.ts
      to: src/lib/bp/medical-disclaimer.ts
      via: "Non-diagnostic boundary consistency"
      pattern: "INTERPRETATION_DISCLAIMER or shared short line"
---

<objective>
Deliver **PRIV-02**: reviewable in-app privacy / data copy (local SQLite, no account/cloud sync/ads v1, PDF user-initiated only, core offline, honest Android backup line after 05-02). Wire Settings → new stack screen; dedupe with `tokens.ts` where Settings rows duplicate short lines.

Purpose: Users can find truth without re-running first-run gate.
Output: RNTL test for navigation + screen content smoke; `npx tsc --noEmit` green.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-privacy-and-release-hardening/05-CONTEXT.md
@.planning/phases/05-privacy-and-release-hardening/05-UI-SPEC.md
@.planning/phases/04-pdf-export/04-CONTEXT.md
@src/features/onboarding/privacy-copy.ts
@src/app/privacy.tsx
@src/app/(tabs)/settings.tsx
@src/lib/theme/tokens.ts
@src/lib/bp/medical-disclaimer.ts
@src/app/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: privacy-detail-copy module</name>
  <read_first>
    - src/features/onboarding/privacy-copy.ts
    - src/lib/bp/medical-disclaimer.ts
    - .planning/phases/05-privacy-and-release-hardening/05-UI-SPEC.md
  </read_first>
  <files>src/features/privacy/privacy-detail-copy.ts</files>
  <action>Create `src/features/privacy/privacy-detail-copy.ts` exporting: screen title string; array of `{ title: string; body: string }` sections (at least: local storage / SQLite on device; what we do not do — no account, no cloud sync, no ads in v1; export — PDF only when user generates and shares; network — core logging/history/trends/export work offline, no analytics/ad SDKs shipped in this app per dependency review); Android backup — state that **automatic Android backup to Google is turned off for this app** (consistent with `allowBackup: false` from 05-02); device transfer / new device — plain language that moving phones is OS/account dependent. Reuse or align wording with `PRIVACY_BODY` / settings `copy` where appropriate. Import `INTERPRETATION_DISCLAIMER` or one short non-diagnostic sentence for a final note — do not contradict Phase 2 medical boundary.</action>
  <acceptance_criteria>
    - File exports a title constant and a sections array (or equivalent structure) consumable by a screen.
    - At least one section body contains the substring `PDF` or `export` (case-sensitive OK).
    - At least one section mentions backup or `Android` in honest alignment with `allowBackup: false`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Long-form copy module exists.</done>
</task>

<task type="auto">
  <name>Task 2: privacy-info stack screen</name>
  <read_first>
    - src/app/export-report.tsx
    - src/components/ui/ScreenContainer.tsx
  </read_first>
  <files>src/app/privacy-info.tsx, src/app/_layout.tsx</files>
  <action>Add `src/app/privacy-info.tsx` as a stack screen with `headerShown: true`, title from privacy-detail title constant. Use `ScreenContainer` + `ScrollView`; map sections to `Text` (title `typography.label` or heading style, body `typography.body`). Register `<Stack.Screen name="privacy-info" options={{ headerShown: true, title: … }} />` in `_layout.tsx` after other stack screens. Use theme `colors`, `spacing`.</action>
  <acceptance_criteria>
    - `_layout.tsx` includes the literal substring `privacy-info`.
    - `privacy-info.tsx` imports from `privacy-detail-copy.ts`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Privacy detail route exists.</done>
</task>

<task type="auto">
  <name>Task 3: Settings navigation + copy alignment</name>
  <read_first>
    - src/app/(tabs)/settings.tsx
    - src/lib/theme/tokens.ts
  </read_first>
  <files>src/app/(tabs)/settings.tsx, src/lib/theme/tokens.ts</files>
  <action>Add `copy.settingsPrivacyLabel` / `copy.settingsPrivacyDetail` (or repurpose `settingsAboutLabel` to **Privacy** if product prefers — UI-SPEC allows Privacy or About). Wire `InfoRow` with `showChevron`, `onPress={() => router.push('/privacy-info')}`, `testID="settings-privacy"`. Remove dead chevron on About if replaced. Optionally shorten `settingsLocalDetail` to avoid contradicting long-form; ensure footnote still accurate.</action>
  <acceptance_criteria>
    - `settings.tsx` contains `router.push('/privacy-info')` or `href: '/privacy-info'`.
    - `tokens.ts` `copy` includes new strings used by the row.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Settings links to privacy detail.</done>
</task>

<task type="auto">
  <name>Task 4: RNTL smoke test</name>
  <read_first>
    - src/features/onboarding/__tests__/privacy-gate.test.tsx
    - jest setup if any
  </read_first>
  <files>src/features/privacy/__tests__/privacy-info-screen.test.tsx</files>
  <action>Add test: render `privacy-info` default export (or screen component) with mocked router if needed; assert at least one section title from `privacy-detail-copy` appears. Alternatively integration-style render with Expo Router testing utilities if already used in repo — follow existing test patterns.</action>
  <acceptance_criteria>
    - New test file passes: `npm test -- --runInBand --watchAll=false --testPathPattern=privacy-info-screen`.
    - Full `npm test -- --runInBand --watchAll=false` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=privacy-info-screen</automated>
  </verify>
  <done>Automated coverage for privacy detail screen.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Copy | Factual claims must match manifest (backup off) and no fabricated collection |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-1 | I | Misleading privacy text | Mitigate | Align with RESEARCH + app.json; legal-adjacent accuracy |
| T-05-01-2 | T | None new | Accept | Read-only screen |
</threat_model>

<verification>
`npm test -- --runInBand --watchAll=false`; `npx tsc --noEmit`.
</verification>

<success_criteria>
PRIV-02 satisfied: discoverable long-form privacy + data screen; Settings wired; tests green.
</success_criteria>

<output>
After execution, append `05-01-SUMMARY.md` per GSD summary template when required by workflow.
</output>
