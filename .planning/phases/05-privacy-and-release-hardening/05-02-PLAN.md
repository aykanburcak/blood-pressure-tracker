---
phase: 05-privacy-and-release-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
must_haves:
  truths:
    - "Expo android config sets allowBackup false so Android Auto Backup does not upload app data including SQLite."
    - "Behavior matches in-app promise that readings are not cloud-synced by the app; OS backup path is disabled at app level."
  artifacts:
    - path: app.json
      provides: "expo.android.allowBackup false"
  key_links:
    - from: app.json
      to: .planning/phases/05-privacy-and-release-hardening/05-CONTEXT.md
      via: "D-04 backup exclusion"
      pattern: "allowBackup"
---

<objective>
Disable Android Auto Backup for the whole app via Expo `app.json` so local SQLite and other app data are not uploaded to Google backup by default — aligns shipped behavior with privacy positioning (05-CONTEXT D-04).

Purpose: Implement the native boundary before long-form copy references it (05-01).
Output: `npx expo config --type public` shows `android.allowBackup` false (or equivalent merged manifest intent).
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-privacy-and-release-hardening/05-CONTEXT.md
@.planning/phases/05-privacy-and-release-hardening/05-RESEARCH.md
@app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set android.allowBackup false in app.json</name>
  <read_first>
    - app.json
    - .planning/phases/05-privacy-and-release-hardening/05-RESEARCH.md
  </read_first>
  <files>app.json</files>
  <action>Under `expo.android`, add `"allowBackup": false`. Preserve existing `android` keys (`adaptiveIcon`, `predictiveBackGestureEnabled`, etc.). If `android` is missing keys after edit, keep JSON valid. Do not add raw `android/` folder — CNG only.</action>
  <acceptance_criteria>
    - `app.json` contains `"allowBackup"` nested under the `android` object inside `expo` with boolean value `false`.
    - `npx expo config --type public` exits 0 (run from repo root).
  </acceptance_criteria>
  <verify>
    <automated>npx expo config --type public</automated>
  </verify>
  <done>Android backup disabled via Expo config.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Manifest | Only backup flag; no new network endpoints |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-1 | I | Backup exfiltration | Mitigate | `allowBackup: false` reduces Google backup exposure for app data |
| T-05-02-2 | E | Misconfiguration | Mitigate | Verify with `expo config` after edit |
</threat_model>

<verification>
`npx expo config --type public` succeeds; `app.json` valid JSON.
</verification>

<success_criteria>
Android Auto Backup is off for the application per Expo prebuild inputs.
</success_criteria>

<output>
After execution, append `05-02-SUMMARY.md` per GSD summary template when required by workflow.
</output>
