---
phase: 02-core-logging-and-interpretation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - drizzle.config.ts
  - drizzle/meta/_journal.json
  - drizzle/*.sql
  - src/lib/db/schema.ts
  - src/lib/db/index.ts
  - src/lib/db/migrate.ts
  - src/lib/db/readings-repository.ts
  - src/lib/db/__tests__/readings-repository.test.ts
autonomous: true
requirements:
  - HIST-04
must_haves:
  truths:
    - "Readings persist in SQLite in a dedicated database file separate from app-shell.db."
    - "A versioned migration creates the readings table before any insert."
    - "getLatestReading returns the most recent row by measuredAt for Home and HIST-04."
  artifacts:
    - path: src/lib/db/schema.ts
      provides: "Drizzle readings table definition"
    - path: drizzle/
      provides: "Generated SQL migrations for readings"
    - path: src/lib/db/readings-repository.ts
      provides: "insertReading and getLatestReading APIs"
    - path: src/lib/db/__tests__/readings-repository.test.ts
      provides: "Automated proof of insert + latest round-trip"
  key_links:
    - from: src/lib/db/migrate.ts
      to: src/lib/db/readings-repository.ts
      via: "migrations must run before repository methods open the DB"
      pattern: "migrate"
---

<objective>
Introduce Drizzle ORM with Expo SQLite for the `readings` domain: schema, generated migrations, startup migration runner, and a small repository with `insertReading` and `getLatestReading`.

Purpose: Give Phase 2 UI plans a durable persistence boundary separate from `src/lib/storage/app-shell-flags.ts` (D-17, D-18).
Output: New `readings.db` file name constant, checked-in `drizzle/` migrations, green repository tests.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-core-logging-and-interpretation/02-CONTEXT.md
@.planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
@.planning/phases/02-core-logging-and-interpretation/02-VALIDATION.md
@src/lib/storage/app-shell-flags.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Drizzle dependencies and drizzle-kit configuration</name>
  <read_first>
    - package.json
    - .planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
    - AGENTS.md
  </read_first>
  <files>package.json, drizzle.config.ts</files>
  <action>Add runtime dependency `drizzle-orm` and devDependency `drizzle-kit` using versions compatible with Expo SDK 55 (pin via `npm install drizzle-orm` and `npm install -D drizzle-kit`, then align with Expo if the install resolver conflicts). Create `drizzle.config.ts` at the repo root with `defineConfig` from `drizzle-kit`, `schema: './src/lib/db/schema.ts'`, `out: './drizzle'`, and `dialect: 'sqlite'`. Add an npm script `"db:generate": "drizzle-kit generate"` to `package.json`. Do not add Prisma, Supabase, or server database clients.</action>
  <acceptance_criteria>
    - `package.json` lists `drizzle-orm` in `dependencies` and `drizzle-kit` in `devDependencies`.
    - `drizzle.config.ts` exists and contains the literal substrings `schema`, `src/lib/db/schema.ts`, `out`, `drizzle`, and `dialect`.
    - `package.json` contains a `db:generate` script that invokes `drizzle-kit generate`.
    - `rg -n "prisma|@supabase" package.json` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm install</automated>
  </verify>
  <done>Drizzle tooling is installed and configured for SQLite schema generation.</done>
</task>

<task type="auto">
  <name>Task 2: Define the readings table schema in Drizzle</name>
  <read_first>
    - src/lib/db/schema.ts
    - .planning/phases/02-core-logging-and-interpretation/02-CONTEXT.md
    - drizzle.config.ts
  </read_first>
  <files>src/lib/db/schema.ts, src/lib/db/index.ts</files>
  <action>Create `src/lib/db/schema.ts` exporting a Drizzle SQLite table named `readings` with columns: `id` TEXT PRIMARY KEY NOT NULL (store string UUIDs), `systolic` INTEGER NOT NULL, `diastolic` INTEGER NOT NULL, `pulse` INTEGER (nullable), `measuredAt` INTEGER NOT NULL (Unix epoch milliseconds), `createdAt` INTEGER NOT NULL (set on insert in repository or DB default). Export typed table and inferred insert/select types. Add `src/lib/db/index.ts` that re-exports `schema` symbols needed by the repository. Use `sqliteTable` from `drizzle-orm/sqlite-core`.</action>
  <acceptance_criteria>
    - `src/lib/db/schema.ts` contains the identifier `readings` and the column names `systolic`, `diastolic`, `pulse`, `measuredAt`, `createdAt`.
    - `src/lib/db/schema.ts` imports from `drizzle-orm/sqlite-core`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Readings schema exists as the single source of truth for migrations.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Generate and commit the initial SQLite migration</name>
  <read_first>
    - drizzle.config.ts
    - src/lib/db/schema.ts
    - .planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
  </read_first>
  <files>drizzle/*.sql, drizzle/meta/_journal.json</files>
  <action>Run `npm run db:generate` (or `npx drizzle-kit generate`) so Drizzle emits SQL under `drizzle/` including `meta/_journal.json`. The generated SQL must create the `readings` table matching the schema. Commit-ready files must exist in the repo — this task is **[BLOCKING]** for any insert: downstream code must not hand-write `CREATE TABLE` outside the migration bundle. If the generator names the file with a numeric prefix, keep Drizzle defaults.</action>
  <acceptance_criteria>
    - Directory `drizzle/` exists and contains at least one `.sql` file whose text includes `readings` and `CREATE TABLE`.
    - `drizzle/meta/_journal.json` exists and references the new migration entry.
    - `rg -l "readings" drizzle/*.sql` returns at least one file path.
  </acceptance_criteria>
  <verify>
    <automated>test -f drizzle/meta/_journal.json && ls drizzle/*.sql | head -1 | xargs grep -q "readings"</automated>
  </verify>
  <done>Initial migration artifacts exist for bundled app migrations.</done>
</task>

<task type="auto">
  <name>Task 4: Readings DB client, migrator, and repository functions</name>
  <read_first>
    - src/lib/storage/app-shell-flags.ts
    - .planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
    - drizzle/meta/_journal.json
    - src/lib/db/schema.ts
  </read_first>
  <files>src/lib/db/migrate.ts, src/lib/db/readings-repository.ts</files>
  <action>Implement a dedicated SQLite database file constant `readings.db` (must not be `app-shell.db`). Use Expo SQLite async API compatible with Drizzle’s Expo integration from the official Drizzle Expo guide (`drizzle-orm/expo-sqlite` + `openDatabaseAsync`). In `src/lib/db/migrate.ts`, export `runReadingsMigrations(db)` that applies migrations from the bundled `drizzle` folder using Drizzle’s `migrate` helper. In `src/lib/db/readings-repository.ts`, export `getReadingsDb()` that opens `readings.db`, awaits migrations once per open, and returns a Drizzle database instance; export `insertReading(input)` inserting a row with a new UUID string for `id` and `createdAt = Date.now()`; export `getLatestReading()` selecting the row with maximum `measuredAt` (tie-break by `createdAt` descending if needed). Use only Drizzle query builders for inserts/selects — no string-concatenated SQL in application code. Do not read or write the `app_flags` table from this module.</action>
  <acceptance_criteria>
    - `src/lib/db/readings-repository.ts` contains the substring `readings.db` and does not contain `app-shell.db`.
    - `src/lib/db/migrate.ts` imports `migrate` from `drizzle-orm/expo-sqlite/migrator` or the documented equivalent for the installed Drizzle version.
    - `insertReading` and `getLatestReading` are exported from `src/lib/db/readings-repository.ts`.
    - `rg "execAsync\\(\\s*\`CREATE TABLE readings" src/lib/db` returns no matches (no ad-hoc create in TS).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=readings-repository</automated>
  </verify>
  <done>Repository can insert and fetch latest reading against migrated SQLite.</done>
</task>

<task type="auto">
  <name>Task 5: Automated tests for repository round-trip</name>
  <read_first>
    - src/lib/storage/__tests__/app-shell-flags.test.ts
    - src/lib/db/readings-repository.ts
    - jest.config.js
  </read_first>
  <files>src/lib/db/__tests__/readings-repository.test.ts</files>
  <action>Add Jest tests that open the readings DB (use a dedicated test database filename such as `readings-test.db` passed via an optional parameter or internal test helper to avoid clashing with `readings.db` if the implementation allows; if not, document cleanup between tests). Test A: after `runReadingsMigrations`, `insertReading` with systolic 120, diastolic 80, pulse null, measuredAt now returns an id. Test B: `getLatestReading` returns that row. Test C: insert a second row with a later `measuredAt` and assert `getLatestReading` returns the second row. Tests must run under `jest-expo` without a network.</action>
  <acceptance_criteria>
    - File `src/lib/db/__tests__/readings-repository.test.ts` exists and contains at least two `it(` blocks.
    - `npm test -- --runInBand --watchAll=false --testPathPattern=readings-repository` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=readings-repository</automated>
  </verify>
  <done>HIST-04 persistence layer is test-covered independent of UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App code → SQLite file | Malformed SQL or dual writers could corrupt local health data |
| Migrations → schema | Skipped migrations would break inserts or upgrades |
| Tests → production DB filename | Tests must not destroy real user `readings.db` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `readings-repository.ts` | mitigate | Use Drizzle query builders / prepared patterns only; forbid dynamic SQL concatenation (acceptance grep) |
| T-02-02 | D | migrations | mitigate | [BLOCKING] generate and bundle migrations; run before inserts |
| T-02-03 | E | test suite | mitigate | Use isolated `readings-test.db` or equivalent cleanup strategy in tests |
| T-02-04 | I | local DB file | accept | Single-user offline threat model; optional device encryption is OS-level |
</threat_model>

<verification>
Run `npm test -- --runInBand --watchAll=false` and confirm `drizzle/*.sql` plus `readings-repository` tests pass.
</verification>

<success_criteria>
Readings are stored in `readings.db` with Drizzle migrations applied, and automated tests prove insert plus latest selection.
</success_criteria>

<output>
After execution, append a short `02-01-SUMMARY.md` per GSD summary template if the milestone workflow expects it.
</output>
