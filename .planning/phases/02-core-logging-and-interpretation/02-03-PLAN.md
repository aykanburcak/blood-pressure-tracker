---
phase: 02-core-logging-and-interpretation
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - package.json
  - src/app/_layout.tsx
  - src/app/add-reading.tsx
  - src/lib/theme/tokens.ts
  - src/features/readings/AddReadingScreen.tsx
  - src/app/(tabs)/index.tsx
  - src/features/readings/__tests__/add-reading-screen.test.tsx
  - src/app/(tabs)/__tests__/home-latest-reading.test.tsx
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - INTP-01
  - INTP-02
  - INTP-03
must_haves:
  truths:
    - "User can open Add reading from Home, enter values, pick date/time, and save with one primary action."
    - "Invalid or out-of-range data never calls insertReading."
    - "Home shows the latest stored reading with WHO label, color chip, and disclaimer after save and on remount."
  artifacts:
    - path: src/app/add-reading.tsx
      provides: "Routed add-reading entry"
    - path: src/features/readings/AddReadingScreen.tsx
      provides: "Form + picker + save wiring"
    - path: src/app/(tabs)/index.tsx
      provides: "Home CTA + latest-reading hero state"
    - path: src/features/readings/__tests__/add-reading-screen.test.tsx
      provides: "LOG-* regression coverage"
    - path: src/app/(tabs)/__tests__/home-latest-reading.test.tsx
      provides: "INTP-* dashboard coverage"
  key_links:
    - from: AddReadingScreen
      to: src/lib/db/readings-repository.ts
      via: "insertReading after readingInputSchema.safeParse success"
      pattern: "insertReading"
    - from: src/app/(tabs)/index.tsx
      to: src/lib/db/readings-repository.ts
      via: "getLatestReading on focus/mount"
      pattern: "getLatestReading"
---

<objective>
Ship the add-reading flow and Home latest-reading experience: stack route, native date/time picker, Zod-gated save into SQLite, and dashboard summary with WHO interpretation, colors, and disclaimer.

Purpose: Close LOG-01/02/04, INTP-02/03, and wire LOG-03 enforcement at the save boundary.
Output: Green Jest suite for new screens; optional manual relaunch check per VALIDATION.md.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-logging-and-interpretation/02-CONTEXT.md
@.planning/phases/02-core-logging-and-interpretation/02-UI-SPEC.md
@.planning/phases/02-core-logging-and-interpretation/02-VALIDATION.md
@src/app/_layout.tsx
@src/app/(tabs)/index.tsx
@src/components/ui/PrimaryButton.tsx
@src/components/ui/ScreenContainer.tsx
@src/components/ui/SurfaceCard.tsx
@src/lib/db/readings-repository.ts
@src/lib/bp/reading-schema.ts
@src/lib/bp/who-classification.ts
@src/lib/bp/medical-disclaimer.ts
@src/lib/bp/interpretation-style.ts
@src/test/render.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install DateTimePicker via Expo</name>
  <read_first>
    - package.json
    - .planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
  </read_first>
  <files>package.json</files>
  <action>Run `npx expo install @react-native-community/datetimepicker` so the native module version matches Expo SDK 55. Do not add `react-native-modal-datetime-picker` unless Expo install fails and research documents a blocker.</action>
  <acceptance_criteria>
    - `package.json` includes `@react-native-community/datetimepicker` in `dependencies`.
    - `npm install` completes without peer dependency errors that block `expo start`.
  </acceptance_criteria>
  <verify>
    <automated>npm install</automated>
  </verify>
  <done>LOG-02 native picker dependency is available.</done>
</task>

<task type="auto">
  <name>Task 2: Register the add-reading stack screen</name>
  <read_first>
    - src/app/_layout.tsx
    - .planning/phases/01-foundation-and-offline-shell/01-UI-SPEC.md
    - .planning/phases/02-core-logging-and-interpretation/02-UI-SPEC.md
  </read_first>
  <files>src/app/_layout.tsx, src/app/add-reading.tsx</files>
  <action>Update root `Stack` in `src/app/_layout.tsx` to include `<Stack.Screen name="add-reading" options={{ headerShown: true, title: 'New reading', presentation: 'card' }} />` or equivalent so the screen has a visible back control (adjust `headerShown`/`title` to match platform norms if `false` was required elsewhere — **must** allow dismissing back to tabs). Create `src/app/add-reading.tsx` that default-exports the feature screen component from `src/features/readings/AddReadingScreen.tsx` (thin route file). Do not nest add-reading inside `(tabs)`.</action>
  <acceptance_criteria>
    - `src/app/_layout.tsx` contains the substring `add-reading`.
    - `src/app/add-reading.tsx` exists and imports from `AddReadingScreen`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Add-reading is reachable as a stack overlay above tabs.</done>
</task>

<task type="auto">
  <name>Task 3: Implement AddReadingScreen form, validation, and save</name>
  <read_first>
    - .planning/phases/02-core-logging-and-interpretation/02-UI-SPEC.md
    - src/lib/bp/reading-schema.ts
    - src/lib/db/readings-repository.ts
    - src/components/ui/PrimaryButton.tsx
    - src/components/ui/ScreenContainer.tsx
  </read_first>
  <files>src/features/readings/AddReadingScreen.tsx</files>
  <action>Build `AddReadingScreen` using `ScreenContainer`, labels per UI-SPEC (`Systolic`, `Diastolic`, `Pulse (optional)`, `Date and time`), numeric `TextInput`s for BP fields, optional pulse, and `@react-native-community/datetimepicker` toggled from a pressable summary row showing the current `measuredAt`. Initialize `measuredAt` state to `new Date()`. On `Save reading`, run `readingInputSchema.safeParse`; on failure, set field-level error strings from Zod `flatten()`. On success, call `insertReading` mapping pulse `undefined` to SQL `NULL`, `measuredAt` to epoch ms, then `router.back()`. Disable the save button while an async save is in flight. Do not call `insertReading` when parse fails.</action>
  <acceptance_criteria>
    - `AddReadingScreen.tsx` imports `readingInputSchema` from `src/lib/bp/reading-schema` and `insertReading` from the readings repository.
    - File contains the exact button label `Save reading` (UI-SPEC).
    - `rg -n "insertReading" src/features/readings/AddReadingScreen.tsx` shows insert only after validation (executor documents pattern: single call site inside success branch).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=add-reading-screen</automated>
  </verify>
  <done>LOG-01/02/04 and LOG-03 save gate are implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Home tab — Add reading CTA and latest-reading summary</name>
  <read_first>
    - src/app/(tabs)/index.tsx
    - .planning/phases/02-core-logging-and-interpretation/02-UI-SPEC.md
    - src/lib/theme/tokens.ts
    - src/lib/db/readings-repository.ts
    - src/lib/bp/who-classification.ts
    - src/lib/bp/medical-disclaimer.ts
    - src/lib/bp/interpretation-style.ts
  </read_first>
  <files>src/app/(tabs)/index.tsx, src/lib/theme/tokens.ts</files>
  <action>Add `copy.logReadingCta = 'Add reading'` to `tokens.ts` `copy` object (or equivalent constant exported from theme). On Home, render a `PrimaryButton` (or UI-SPEC-approved primary control) labeled `Add reading` that calls `router.push('/add-reading')`. Load `getLatestReading()` on mount and whenever the screen regains focus using `useFocusEffect` from `@react-navigation/native`. If no row: keep the Phase 1 empty hero content. If row: replace the inner empty panel with SBP/DBP display, optional pulse line, formatted `measuredAt`, a chip showing `classifyBloodPressure` label with background from `getInterpretationColor`, and `INTERPRETATION_DISCLAIMER` as secondary body text below. Use existing typography tokens.</action>
  <acceptance_criteria>
    - `src/app/(tabs)/index.tsx` imports `useFocusEffect` and calls `getLatestReading`.
    - `src/app/(tabs)/index.tsx` contains the literal `Add reading` CTA label.
    - `src/lib/theme/tokens.ts` includes `logReadingCta: 'Add reading'` inside `copy` (or grep proves the string is centralized in `copy`).
    - `INTERPRETATION_DISCLAIMER` text appears in the Home file or imported from `medical-disclaimer.ts`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=home-latest-reading</automated>
  </verify>
  <done>INTP-02 and INTP-03 behaviors are wired on Home.</done>
</task>

<task type="auto">
  <name>Task 5: Automated tests for add-reading and Home summary</name>
  <read_first>
    - src/features/onboarding/__tests__/privacy-gate.test.tsx
    - src/test/render.tsx
    - src/lib/db/readings-repository.ts
  </read_first>
  <files>src/features/readings/__tests__/add-reading-screen.test.tsx, src/app/(tabs)/__tests__/home-latest-reading.test.tsx</files>
  <action>Add RNTL tests. **add-reading-screen.test.tsx**: mock `insertReading` from the repository (jest.mock) to resolve with a fixed id; fill valid systolic/diastolic, press `Save reading`, assert mock called once with expected numeric fields; second case with systolic `50` assert mock **not** called and an error message visible. **home-latest-reading.test.tsx**: mock `getLatestReading` to return a row matching schema; after rendering Home, assert SBP/DBP text and WHO label substring appear; mock `getLatestReading` to return `null` and assert `No readings yet` still visible. Use the shared `render` helper from `src/test/render.tsx` and wrap with Expo Router test provider if existing tests do.</action>
  <acceptance_criteria>
    - Both test files exist under the paths in `files`.
    - `npm test -- --runInBand --watchAll=false` exits 0 for the full suite.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false</automated>
  </verify>
  <done>Phase 2 UI paths have automated regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → repository | Skipping Zod could write disallowed tuples |
| Home → latest row | Stale cache could mislead users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | AddReadingScreen | mitigate | `safeParse` gate before `insertReading`; tests assert no call on invalid |
| T-02-05 | S | Home summary | mitigate | Disclaimer always rendered with interpretation; labels from shared classifier |
| T-02-06 | E | Navigation | mitigate | Use typed `router.push('/add-reading')` and `router.back()` only after success |
</threat_model>

<verification>
Run full `npm test -- --runInBand --watchAll=false`. Optionally perform manual HIST-04 relaunch check from `02-VALIDATION.md`.
</verification>

<success_criteria>
Users can record a reading with validation and see the latest interpretation on Home with correct non-diagnostic framing.
</success_criteria>

<output>
After execution, add `02-03-SUMMARY.md` per GSD conventions when summarizing the wave.
</output>
