---
phase: 02-core-logging-and-interpretation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/bp/reading-schema.ts
  - src/lib/bp/who-classification.ts
  - src/lib/bp/medical-disclaimer.ts
  - src/lib/bp/interpretation-style.ts
  - src/lib/theme/tokens.ts
  - src/lib/bp/__tests__/reading-schema.test.ts
  - src/lib/bp/__tests__/who-classification.test.ts
autonomous: true
requirements:
  - LOG-03
  - INTP-01
must_haves:
  truths:
    - "Zod rejects out-of-range and inconsistent BP inputs before any save."
    - "WHO-style classification is a pure function with exhaustive unit tests for each band."
    - "Interpretation colors and disclaimer copy exist as tokens/constants for UI reuse."
  artifacts:
    - path: src/lib/bp/reading-schema.ts
      provides: "Zod schema for systolic, diastolic, optional pulse, measuredAt"
    - path: src/lib/bp/who-classification.ts
      provides: "classifyBloodPressure(systolic, diastolic) → category + label"
    - path: src/lib/bp/medical-disclaimer.ts
      provides: "Short non-diagnostic disclaimer string matching 02-UI-SPEC"
    - path: src/lib/theme/tokens.ts
      provides: "interpretation color tokens per UI spec"
  key_links:
    - from: src/lib/bp/reading-schema.ts
      to: src/lib/bp/who-classification.ts
      via: "validated integers feed classifier after successful parse"
      pattern: "systolic"
---

<objective>
Implement validation and interpretation domain logic: Zod schema for reading inputs, deterministic WHO-style blood pressure classification, medical disclaimer copy, and interpretation color tokens.

Purpose: Keep clinical-adjacent rules testable without React (LOG-03, INTP-01, INTP-02).
Output: Pure modules + Jest coverage; no navigation or SQLite in this plan.
</objective>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-logging-and-interpretation/02-CONTEXT.md
@.planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
@.planning/phases/02-core-logging-and-interpretation/02-UI-SPEC.md
@.planning/phases/02-core-logging-and-interpretation/02-VALIDATION.md
@src/lib/theme/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Zod dependency</name>
  <read_first>
    - package.json
    - AGENTS.md
  </read_first>
  <files>package.json</files>
  <action>Install `zod` with `npm install zod` targeting the 4.x line compatible with the project TypeScript version. Do not add Yup, Joi, or class-validator.</action>
  <acceptance_criteria>
    - `package.json` dependencies include `"zod"` with a semver range.
    - `rg -n "\"yup\"|\"joi\"" package.json` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm install</automated>
  </verify>
  <done>Zod is available for boundary validation.</done>
</task>

<task type="auto">
  <name>Task 2: Zod reading input schema with plausibility ranges</name>
  <read_first>
    - .planning/phases/02-core-logging-and-interpretation/02-CONTEXT.md
    - .planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
    - .planning/phases/02-core-logging-and-interpretation/02-UI-SPEC.md
  </read_first>
  <files>src/lib/bp/reading-schema.ts, src/lib/bp/__tests__/reading-schema.test.ts</files>
  <action>Create `src/lib/bp/reading-schema.ts` exporting a Zod schema `readingInputSchema` for an object with: `systolic` number coerced from string optional input path if needed (executor chooses int-only or safe coercion), integer range **70–250** inclusive; `diastolic` **40–150** inclusive; `pulse` optional — if present, **30–220** inclusive; `measuredAt` as `z.date()` or `z.coerce.date()` from ISO or number (document in code). Add `.superRefine` so that **diastolic must be strictly less than systolic** when both are numbers. Export inferred type `ReadingInput`. Add tests in `reading-schema.test.ts` that pass for `{120,80,undefined,validDate}`, fail for empty systolic, fail for diastolic ≥ systolic, fail for systolic 69, fail for pulse 10 when provided.</action>
  <acceptance_criteria>
    - `src/lib/bp/reading-schema.ts` exports `readingInputSchema` and a type alias derived from `z.infer`.
    - `src/lib/bp/__tests__/reading-schema.test.ts` contains at least five `it(` blocks covering the cases listed in the action.
    - `npm test -- --runInBand --watchAll=false --testPathPattern=reading-schema` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=reading-schema</automated>
  </verify>
  <done>LOG-03 validation rules are encoded and tested.</done>
</task>

<task type="auto">
  <name>Task 3: WHO-style blood pressure classification</name>
  <read_first>
    - .planning/phases/02-core-logging-and-interpretation/02-RESEARCH.md
  </read_first>
  <files>src/lib/bp/who-classification.ts, src/lib/bp/__tests__/who-classification.test.ts</files>
  <action>Create `src/lib/bp/who-classification.ts` exporting a const enum or string union `WhoBpCategory` with values: `normal`, `elevated`, `stage1`, `stage2`, `crisis`. Export `classifyBloodPressure(systolic: number, diastolic: number): { category: WhoBpCategory; label: string }` implementing the precedence from `02-RESEARCH.md` (crisis if SBP≥180 OR DBP≥120; else stage2 if SBP≥140 OR DBP≥90; else stage1 if SBP 130–139 OR DBP 80–89; else elevated if SBP 120–129 AND DBP is less than 80; else normal). Use **human-readable labels** suitable for UI chips, e.g. `Normal`, `Elevated`, `High blood pressure (Stage 1)`, `High blood pressure (Stage 2)`, `Hypertensive crisis`. Add unit tests with boundary pairs: (119,79) normal; (120,79) elevated; (129,79) elevated; (130,79) stage1; (139,89) stage1; (140,89) stage2; (180,90) crisis; (120,120) crisis.</action>
  <acceptance_criteria>
    - `classifyBloodPressure` is exported from `src/lib/bp/who-classification.ts`.
    - `who-classification.test.ts` includes assertions for each of the boundary tuples listed in the action.
    - `npm test -- --runInBand --watchAll=false --testPathPattern=who-classification` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runInBand --watchAll=false --testPathPattern=who-classification</automated>
  </verify>
  <done>INTP-01 classification is deterministic and test-covered.</done>
</task>

<task type="auto">
  <name>Task 4: Disclaimer string and interpretation color tokens</name>
  <read_first>
    - .planning/phases/02-core-logging-and-interpretation/02-UI-SPEC.md
    - src/lib/theme/tokens.ts
  </read_first>
  <files>src/lib/bp/medical-disclaimer.ts, src/lib/bp/interpretation-style.ts, src/lib/theme/tokens.ts</files>
  <action>Add `src/lib/bp/medical-disclaimer.ts` exporting `INTERPRETATION_DISCLAIMER` with the exact UI-SPEC string: `This is not a medical diagnosis. Talk to a clinician about your readings.` Extend `src/lib/theme/tokens.ts` `colors` object with keys `interpretNormal`, `interpretElevated`, `interpretStage1`, `interpretStage2`, `interpretCrisis` using the hex values from `02-UI-SPEC.md` (`#22C55E`, `#EAB308`, `#F97316`, `#EA580C`, `#B91C1C`). Add `src/lib/bp/interpretation-style.ts` exporting `getInterpretationColor(category: WhoBpCategory): string` that maps each `WhoBpCategory` from `who-classification.ts` to the matching `colors.interpret*` hex. Avoid import cycles: `interpretation-style.ts` may import `WhoBpCategory` type and `colors` from `tokens.ts` (tokens must not import bp modules).</action>
  <acceptance_criteria>
    - `INTERPRETATION_DISCLAIMER` in `medical-disclaimer.ts` matches the UI-SPEC sentence exactly (verbatim grep).
    - `tokens.ts` defines all five `interpret*` color keys with the exact hex codes from `02-UI-SPEC.md`.
    - `src/lib/bp/interpretation-style.ts` exports `getInterpretationColor` returning a hex string for each `WhoBpCategory`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>INTP-02 presentation inputs exist for the UI plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → validation | Skipping validation could persist dangerous garbage values |
| Classification → UI | Wrong labels could imply clinical authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | T | `reading-schema.ts` | mitigate | Central Zod schema + exhaustive tests |
| T-02-03 | S | `who-classification.ts` | mitigate | Fixed disclaimer string; labels avoid “diagnosis” wording |
| T-02-05 | I | N/A | accept | Local-only classification; no server |
</threat_model>

<verification>
Run `npm test -- --runInBand --watchAll=false --testPathPattern='reading-schema|who-classification'` and `npx tsc --noEmit`.
</verification>

<success_criteria>
Validation and WHO classification are pure, tested, and ready for the add-reading UI to consume without duplicating rules.
</success_criteria>

<output>
After execution, add `02-02-SUMMARY.md` if required by workspace GSD conventions.
</output>
