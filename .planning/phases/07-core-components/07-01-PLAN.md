---
phase: 07-core-components
plan: 01
type: execute
wave: 1
depends_on:
  - phase-06-tokens-and-typography
files_modified:
  - src/lib/theme/tokens.ts
  - src/components/ui/PrimaryButton.tsx
  - src/components/ui/SurfaceCard.tsx
  - src/components/ui/InfoRow.tsx
  - src/lib/bp/interpretation-chip.ts
  - src/app/(tabs)/index.tsx
  - src/app/export-report.tsx
  - src/features/readings/AddReadingScreen.tsx
  - src/features/readings/EditReadingScreen.tsx
requirements:
  - DS-04
  - DS-05
  - DS-06
  - DS-07
autonomous: true
---

<objective>
Deliver DS-04–DS-07: pill primary CTA with on-primary label token, larger-radius cards, Stitch-style secondary surfaces (export presets, date rows, InfoRow press targets), form field backgrounds, and a centralized WHO interpretation chip fill. No tab bar, PDF, or navigation chrome changes.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-07-01 | Chip contrast regression (readability) | Medium | Keep label `textPrimary`; tune alpha in code with comment; manual spot-check on white card. |
| T-07-02 | Primary button low contrast | Low | WCAG pair: `#0058BC` + white label is standard; verify disabled opacity still legible. |
| T-07-03 | Destructive action obscured | Low | Do not restyle delete control as secondary; leave destructive colors. |
</threat_model>

<execution_context>
@/Users/aykanburcak/.codex/get-shit-done/workflows/execute-plan.md
@/Users/aykanburcak/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-core-components/07-CONTEXT.md
@.planning/phases/07-core-components/07-RESEARCH.md
@.planning/phases/07-core-components/07-UI-SPEC.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token — onPrimary</name>
  <read_first>src/lib/theme/tokens.ts, 07-CONTEXT.md</read_first>
  <files>src/lib/theme/tokens.ts</files>
  <action>Add **`onPrimary: '#FFFFFF'`** to the semantic palette and spread into **`colors`** (Stitch on_primary). Export on the flat `colors` object for `PrimaryButton`.</action>
  <acceptance_criteria>`colors.onPrimary` exists; `tsc` passes.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>On-primary token available.</done>
</task>

<task type="auto">
  <name>Task 2: PrimaryButton (DS-04)</name>
  <read_first>src/components/ui/PrimaryButton.tsx, 07-UI-SPEC.md</read_first>
  <files>src/components/ui/PrimaryButton.tsx</files>
  <action>Set **`borderRadius: radius.pill`**, **`backgroundColor: colors.primary`**, label **`color: colors.onPrimary`**. Keep `minHeight`, padding, disabled/pressed opacity per 07-CONTEXT D-04.</action>
  <acceptance_criteria>Component uses `primary` + `onPrimary` + `radius.pill`; no gradient dependency.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Primary CTA matches DS-04.</done>
</task>

<task type="auto">
  <name>Task 3: SurfaceCard (DS-06)</name>
  <read_first>src/components/ui/SurfaceCard.tsx</read_first>
  <files>src/components/ui/SurfaceCard.tsx</files>
  <action>**`borderRadius: radius.lg`**. **`backgroundColor: colors.surfaceContainerLowest`** (or `secondary` if identical — prefer explicit semantic name in code comment if same hex).</action>
  <acceptance_criteria>Card corners 24; white fill unchanged visually.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>SurfaceCard matches DS-06.</done>
</task>

<task type="auto">
  <name>Task 4: Interpretation chip helper + Home (DS-07)</name>
  <read_first>src/lib/bp/interpretation-style.ts, src/app/(tabs)/index.tsx, 07-RESEARCH.md</read_first>
  <files>src/lib/bp/interpretation-chip.ts, src/app/(tabs)/index.tsx</files>
  <action>Add **`interpretation-chip.ts`** exporting e.g. **`getInterpretationChipBackground(category: WhoBpCategory): string`** using `getInterpretationColor` + **~20% alpha** (rgba or 8-digit hex). Use on Home for the status chip **`backgroundColor`**; keep **label `colors.textPrimary`**. Remove hard-coded **`${chipColor}22`** pattern.</action>
  <acceptance_criteria>Single source for chip fill; WHO logic unchanged; `tsc` passes.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>WHO chip matches DS-07.</done>
</task>

<task type="auto">
  <name>Task 5: Export report — presets + date rows (DS-05)</name>
  <read_first>src/app/export-report.tsx, 07-UI-SPEC.md</read_first>
  <files>src/app/export-report.tsx</files>
  <action>**Unselected** preset: **`surfaceContainerHigh`**, **remove border**. **Selected**: primary-tint background (~15% alpha) + **`colors.primary`** label. **Date rows**: **`surfaceContainerHigh`**, **`radius.md`**, no border. Align **`presetChip` radius** with **`radius.md`**.</action>
  <acceptance_criteria>No 1px divider border on default chips; selected state visibly distinct.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Export secondary surfaces match DS-05.</done>
</task>

<task type="auto">
  <name>Task 6: Add + Edit reading — inputs + date rows (DS-05)</name>
  <read_first>src/features/readings/AddReadingScreen.tsx, src/features/readings/EditReadingScreen.tsx</read_first>
  <files>src/features/readings/AddReadingScreen.tsx, src/features/readings/EditReadingScreen.tsx</files>
  <action>**`TextInput`**: **`backgroundColor: colors.surfaceContainerLow`**, **`borderRadius: radius.md`**, no border. **Date `Pressable` rows**: **`surfaceContainerHigh`**, **`radius.md`**. Remove white-only styling that fought Stitch layering.</action>
  <acceptance_criteria>Add and Edit screens share the same field chrome tokens.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>Form chrome matches DS-05/D-12.</done>
</task>

<task type="auto">
  <name>Task 7: InfoRow press target (DS-05)</name>
  <read_first>src/components/ui/InfoRow.tsx, 07-CONTEXT.md</read_first>
  <files>src/components/ui/InfoRow.tsx</files>
  <action>When **`onPress`** is set, wrap pressable style with **`backgroundColor: colors.surfaceContainerHigh`**, **`borderRadius: radius.md`**, horizontal padding consistent with Settings list. Non-pressable rows stay transparent (parent card provides context).</action>
  <acceptance_criteria>Chevron rows in Settings read as recessed tappable surfaces.</acceptance_criteria>
  <verify><automated>npx tsc --noEmit</automated></verify>
  <done>InfoRow matches DS-05.</done>
</task>

<task type="auto">
  <name>Task 8: Tests</name>
  <read_first>jest.config.js, affected tests</read_first>
  <files>—</files>
  <action>Run **`npm test -- --runInBand --watchAll=false`**. Fix failures only if introduced (e.g. copy still present; no brittle color assertions expected).</action>
  <acceptance_criteria>Full Jest green.</acceptance_criteria>
  <verify><automated>npm test -- --runInBand --watchAll=false</automated></verify>
  <done>Regression suite green.</done>
</task>

</tasks>
